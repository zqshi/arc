# v6.19.0 Current — 原生客户端平台扩展（Windows → iOS → 鸿蒙依次推进）

> 状态: 已激活 — 2026-06-29 v6.18 归档后激活, 恢复"有且仅有一个 *-current.md"协议不变量。本版本方向已定: 补齐 BINARY_APP 的 Windows/iOS/鸿蒙 构建目标 (当前仅 linux/web/android-apk 三目标), 消除"签名器齐了但构建目标没跟上"的断链。
> 起始 commit: b7337b0 (v6.18 归档)
> 上一版本: [v6.18.0](v6.18.0-snapshot.md)
> 上下文: v6.18 投产治理升级已归档。当前 BuildTarget 仅 TAURI_LINUX/WEB/CAPACITOR_APK, 签名器 SignerType 已有 APPLE/WINDOWS/ANDROID (v6.1), 但 iOS/Windows/鸿蒙 缺构建目标 → "能签无产物"。本版本依次推进三平台, 先 artifact 显式建模再接构建 runtime (延续 [[feedback_artifact_before_runtime]] 铁律)。

## 版本目标

让 BINARY_APP 项目能构建并签名分发 Windows / iOS / 鸿蒙 原生客户端产物, 对齐已存在的三平台签名器链路。不破坏现有 linux/web/android-apk 三目标, 不改 DDD 领域行为。

## 版本验证标准

1. 新建项目选 BINARY_APP 后, 构建目标下拉可选 Windows / iOS / 鸿蒙 (前端透出)
2. 各平台走通"构建 → 产物建模 → 签名 → 分发"链路 (真实凭证/runner 就绪后端到端验证)
3. 质量检测 6.1-6.7 全过, 现有三目标零回归

## 关键技术约束 (决定方案)

**核心约束: 当前容器化架构无法构建三新平台产物。**

`DockerSandboxRuntime` + Linux builder 镜像 (tauri-builder/web-builder/android-builder) 只能跑 Linux 容器。
- Windows .exe/.msi 需 WebView2 (仅 Windows) → 需 Windows 原生 runner
- iOS .ipa 需 Xcode + xcodebuild → 需 macOS 原生 runner (Docker 在 Linux host 跑不出 iOS 签名产物)
- 鸿蒙 .hap 需 DevEco Command Line Tools → DevEco 工具链容器化支持差, 需原生或专用镜像

tauri-builder.Dockerfile 注释已明记此约束 (v6.0 决策): "macOS .dmg / Windows .exe 需原生 OS, 容器化沙箱无法构建, 推后到原生 runner / CI matrix"。

→ **T1 架构前置** 是三平台共同关键路径: 决定新平台构建走"CI matrix job 产产物 + arc 编排签名" (延续 v6.18 docker-publish.yml builder 镜像发布的 CI 模式) 还是扩展 NativeRunnerRuntime 对接 GHA windows/macos runner。

## 新增 BuildTarget 的 12 处注册点 (每平台都要改)

1. `domain/sandbox/value_objects.py` — BuildTarget 枚举加值; **同步 `domain/sandbox/execution_backend.py` TARGET_BACKENDS 登记执行后端 (DOCKER/CI, T1 新增真相源, 视为第 13 处注册点)**
2. `domain/sandbox/build_images.py` — DEFAULT_BUILD_IMAGES 加 (BINARY_APP, new_target) 映射 + default_sandbox_config
3. `domain/deployment/value_objects.py` — 部署目标/产物平台
4. `domain/deployment/signer.py` — SignerType (iOS/鸿蒙需新增类型)
5. `domain/artifact/value_objects.py` — 产物类型 (.msi/.exe, .ipa, .hap/.app)
6. `application/context/content/registry.py` + `methodology.py` — PROTOTYPE_BUILD_GUIDES 构建指南
7. `application/sandbox/policy_resolver.py` — 策略解析覆盖
8. `application/sandbox/runtime.py` — 运行时构建命令路由
9. `application/artifact/service.py` + `application/deployment/service.py` — 制品/部署编排
10. `infrastructure/deployer/__init__.py` + `base.py` — 部署器注册
11. `infrastructure/signer/__init__.py` — 签名器注册 (新类型)
12. `interface/schemas/project.py` + `interface/routes/project/core.py` — API schema + 前端 `CreateProjectModal.tsx` BUILD_TARGET_OPTIONS + `types/api.ts`

## 任务依赖图

```
T1 原生 runner 执行后端方案选型 (架构前置, 关键路径)
 ├──→ T2 Windows artifact 建模 ──→ T3 Windows 构建目标 ──→ T4 Windows 签名对接
 │                                                         ↓
 ├──→ T5 iOS artifact 建模 ────→ T6 iOS 构建目标 ──→ T7 IOS 签名器 (新) ──┐
 │                                                                       ↓
 └──→ T8 鸿蒙 artifact 建模 ──→ T9 鸿蒙构建目标 ──→ T10 鸿蒙签名器 (新) ──┤
                                                                         ↓
                                              T11 前端透出三新目标 ──→ T12 质量检测归档
```

依次推进: Windows (波次1, 相对最可行) → iOS (波次2, macOS runner) → 鸿蒙 (波次3, 从零)。每平台内部 artifact → 构建 → 签名 串行 (先 artifact 后 runtime 铁律)。

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 (核心) | blocked 项 |
|----|------|---|------|------|----------------|------------|
| T1 | 原生 runner 执行后端方案选型 | P0 | - | done | `domain/sandbox/execution_backend.py` (已立真相源) | - (裁决方案A, 见 T1 小节) |
| T2 | Windows artifact 显式建模 | P0 | T1 | done | `domain/artifact/value_objects.py` (✅ BuildArtifactKind 簇 + 15 单测) | - |
| T3 | Windows 构建目标 (tauri_windows) | P0 | T2 | in_progress | domain基座✅ + 编排骨架✅(c/e/d/f+config, 全量unit2086过); (a)(b)(g) blocked | Windows runner |
| T4 | Windows 签名对接 (signtool) | P1 | T3 | done | _detect_sign_targets 改读 EXTENSION_KIND+signer_for_kind (消费 T2 真相源, +.msi/.app) | Windows EV 证书 (端到端) |
| T5 | iOS artifact 显式建模 (.ipa) | P0 | T1 | pending | `domain/artifact/value_objects.py`, 注册点5 | - |
| T6 | iOS 构建目标 (capacitor_ios) | P0 | T5 | pending | 注册点1/2/6/7/8/12, macOS runner + xcodebuild | macOS runner |
| T7 | IOS 签名器 (新, xcrun + profile) | P1 | T6 | pending | `domain/deployment/signer.py`, `infrastructure/signer/` | Apple Developer 账号 + provisioning |
| T8 | 鸿蒙 artifact 显式建模 (.hap/.app) | P0 | T1 | pending | `domain/artifact/value_objects.py`, 注册点5 | - |
| T9 | 鸿蒙构建目标 (DevEco + hvigorw) | P0 | T8 | pending | 注册点1/2/6/7/8/12, DevEco CLT | DevEco 工具链 |
| T10 | 鸿蒙签名器 (新, 华为证书) | P1 | T9 | pending | `domain/deployment/signer.py`, `infrastructure/signer/` | 华为开发者证书 |
| T11 | 前端 BUILD_TARGET_OPTIONS 透出三新目标 | P1 | T2,T5,T8 | pending | `frontend/.../CreateProjectModal.tsx`, `types/api.ts` | - |
| T12 | 质量检测 + 归档 | P0 | T4,T7,T10,T11 | pending | 全 6.x 检测 | - |

## 各任务实施要点

### T1 — 原生 runner 执行后端方案选型 (架构前置, 关键路径) — ✅ done

**裁决: 方案 A (CI 编排), 精确形态如下。**

方案对比 (基于代码事实审视, 非文档转述):
- **方案 A (CI 编排)** ✅: 构建产物在 GHA matrix job (windows-latest/macos-latest) 产出 (upload-artifact), arc 运行时只编排 (触发 workflow_dispatch → 轮询 run → 下载 artifact → 接入 BUILD artifact 锚点), 不直接执行构建。arc 核心是"编排 + 签名 + 分发"而非实时构建引擎。
- **方案 B (原生 runtime)** ✗: 扩展 SandboxRuntime ABC 加 NativeRunnerRuntime。否决理由: `SandboxRuntime` 契约是 `run_command(params)->str` 同步单命令返回输出 (三实现 ApprovalGate/Docker/OpenSandbox 均遵循), GHA runner 是"触发 dispatch→分钟级异步→下载产物"的 batch 语义, 与同步单命令错配; 且 CI 产物是 .msi/.ipa 文件 (走 upload-artifact+下载) 非 stdout 字符串, 返回值类型错配。硬塞会扭曲 v6.18 刚理清的契约。

**对 current.md 原描述的两处修正 (架构审视):**
1. "延续 v6.18 docker-publish.yml builder 镜像 CI 模式" — 偏差: `docker-publish.yml` 是"构建 builder 镜像" (runs-on ubuntu, docker/build-push), 产物是镜像; 方案 A 需要的是**新增产物构建 workflow** (runs-on windows/macos, upload-artifact), 产物是二进制安装包。docker-publish.yml 仅作 workflow 写法参考, 非直接延续。
2. T1 涉及文件原写"`application/sandbox/runtime.py` + 新模块" — 修正: 方案 A 下 `runtime.py` **零改动** (CI 编排不进 ABC), 真相源新模块在 `domain/sandbox/execution_backend.py`; 后续 T3 立 `application/build/orchestration.py` + `infrastructure/ci/github_actions_client.py`。

**落地骨架 (T1 已立路由真相源, 编排实现留 T3):**
- `domain/sandbox/execution_backend.py` (✅ 已建 + 单测 4 passed): `BuildExecutionBackend` 枚举 (DOCKER/CI) + `TARGET_BACKENDS` 显式全登记 + `target_execution_backend(target)` 查询 (未登记抛 ValueError, 强制同步登记, v6.15 硬不变量精神)。单一真相源, 调用方读它决定走 docker runtime 还是 CI 编排; 不在 SandboxPolicy 加字段 (CI 不污染 docker 专属配置)。
- 挂点确认: v6.9 已建 `BUILD artifact` 为"构建→签名→分发"统一锚点 (`ArtifactService.create_or_update_build(build_target, artifact_path, build_status)`)。CI 编排器下载产物后调此方法即接入既有链路, 与 docker 构建产物同走 `DeployService.deploy → 签名 → 分发`。
- 留 T3: `application/build/orchestration.py` (BuildOrchestrationService: trigger/poll/collect) + `infrastructure/ci/github_actions_client.py` (GHA API: dispatch/poll/download; 不抽象 CIBackend 接口, YAGNI; client 类封装便于未来替换) + 产物构建 workflow (windows/macos/harmony matrix)。

**blocked 边界**: GHA token (actions:write) + windows/macos runner 待就绪。blocked 期 BuildOrchestrationService 用 mock (触发/轮询/下载打桩), 产物建模 + 路由真相源 + CI workflow 模板可先落地, 端到端待凭证 (延续 v6.1 mock 验证签名链路模式)。

### T2/T5/T8 — artifact 显式建模 (每平台第一步, 铁律: 先 artifact 后 runtime)

**T2 ✅ done (Windows)**: 在 `domain/artifact/value_objects.py` 新增 `BuildArtifactKind` 枚举 (构建产物物理形态) + `KIND_SIGNER_TYPE` (形态→签名平台真相源) + `TARGET_ARTIFACT_KINDS` (target→形态集合) + `target_artifact_kinds()`/`signer_for_kind()` 查询。
- Windows 产物形态 `.msi`/`.exe` 建模为 `BuildArtifactKind.MSI/EXE`, 签名链路声明 `KIND_SIGNER_TYPE[MSI/EXE]=SignerType.WINDOWS` (复用 v6.1 signtool, 不新增 SignerType)。
- **架构澄清 (current.md 原混淆)**: "DELIVERABLES_BY_TYPE 注册可见产物" 是误述 — `DELIVERABLES_BY_TYPE` 在 `domain/project/value_objects.py`, 是 `ProjectType→ArtifactType` 交付物清单 (BINARY_APP 已含 build), 与"产物形态(.msi/.exe)"不同维度; Windows 不新增 ArtifactType (仍用 BUILD 锚点), 故该表无需改。产物形态建模落在 `domain/artifact/value_objects.py` 的 `BuildArtifactKind` 簇。
- 价值: 将 `DeployService._detect_sign_targets` 的文件扩展名扫盘 (.exe→WINDOWS) 提升为 domain 显式真相源, T4 让签名路由读 `signer_for_kind()`。
- 全登记不变量 + 跨真相源一致性测试守护: `KIND_SIGNER_TYPE` 全形态登记 / `TARGET_ARTIFACT_KINDS` 全 target 登记 / 与 `execution_backend.TARGET_BACKENDS` 覆盖相同 BuildTarget 集合 (T3 加 TAURI_WINDOWS 时双真相源同步, 测试 `TestCrossSourceConsistency` 守护)。
- 悬空边界: T2 阶段 MSI/EXE + `KIND_SIGNER_TYPE` 仅 domain 内引用 (T4 infra 才读), 属"domain 契约先于 infra 消费" (同 v6.1 SignerType 枚举先行); 现有形态 (DEB/APPIMAGE/APK) 立即被 `TARGET_ARTIFACT_KINDS` 消费, 不悬空。
- 单测 15 passed (`tests/unit/domain/test_artifact_build_kinds.py`)。

- T5 iOS: 产物类型 .ipa, **需新增 SignerType.IOS** (当前 APPLE=mac .app 的 codesign+notarytool, iOS 是 xcrun + provisioning profile + Apple Developer, 流程不同); 届时加 `BuildArtifactKind.IPA` + `KIND_SIGNER_TYPE[IPA]=SignerType.IOS`
- T8 鸿蒙: 产物类型 .hap/.app, **需新增 SignerType.HARMONY** (华为 .p12 + .cer + .profile, 全新流程); 届时加 `BuildArtifactKind.HAP/APP` + `KIND_SIGNER_TYPE[...]=SignerType.HARMONY`

每平台在 `domain/artifact/value_objects.py` 的 `BuildArtifactKind` + `KIND_SIGNER_TYPE` + `TARGET_ARTIFACT_KINDS` 登记产物形态与签名链路 (T2 已立模式, T5/T8 跟随)。

### T3/T6/T9 — 构建目标 (BuildTarget + builder 镜像/runner + 12 处注册)

**T3 Windows — domain 基座✅ + 编排骨架✅ / (a)(b)(g) blocked (需 runner)**:
- ✅ domain 基座: `BuildTarget.TAURI_WINDOWS` 枚举值已加 (注册点1); `execution_backend.TARGET_BACKENDS[TAURI_WINDOWS]=CI` (注册点13); `artifact.TARGET_ARTIFACT_KINDS[TAURI_WINDOWS]={MSI,EXE}` (注册点5)。跨真相源测试 (`TestCrossSourceConsistency`) 守护双登记一致。配套单测 TAURI_WINDOWS→CI + {MSI,EXE} 通过。
- ✅ **编排骨架 (c/d/e/f + config done, mock 验证)**: (c) `methodology.py` BINARY_APP_BUILD_GUIDE 更新 (注册点6, 去"Windows 不在范围"改"CI 编排激活"); (e) `infrastructure/ci/github_actions_client.py` GitHubActionsClient (dispatch/list_runs/get_run/list_artifacts/download_artifact, transport 可注入, 9 单测 MockTransport); (d) `application/build/orchestration.py` BuildOrchestrationService (dispatch→poll→download/extract→ArtifactService.create_or_update_build 接入 BUILD artifact, 5 单测 fake client+mock; orchestrate 接 `source_url` 参数 = CI 代码投递契约, 调用方 g 上传产生); (f) `.github/workflows/build-client-artifacts.yml` (workflow_dispatch: build_target+source_url+ref, matrix windows, upload-artifact); config.py + .env.example 加 gha_token/gha_owner/gha_repo (6.3 对齐)。全量 unit 2086 passed 无回归。
- ⚠️ **剩余 (a)(b)(g) blocked (需 runner, 三者语义耦合不可独立做)**: T3 真实接缝是 Agent 构建执行路径改造 — 现有 `execution_engine.py:384` 对 `sandbox_policy.mode!=none` 创建 SandboxRuntime, Agent 经 run_command 在 docker **同步执行** build_command; Windows 走 CI 意味构建改 CI **异步编排** (dispatch→poll→download)。触及 (a) policy_resolver 对 CI target 返 NONE policy (注册点7); (b) DeployConfig.for_type 加 TAURI_WINDOWS (注册点3); (g) execution_engine 构建入口对 CI target 拦截走 BuildOrchestrationService + 上传项目代码产 source_url (Agent→CI 触发, 最大改造点)。三者语义耦合 (policy/部署配置的 CI target 语义依赖 execution_engine 怎么拦截构建走编排), 独立做会半截 (policy 处理了 CI target 但 execution_engine 没拦截 → Agent 宿主跑 CI target build_command 失败), 须与 (g) 一起留 runner 端到端验证。current.md 原"T3=加枚举+注册"低估了 Agent→CI 接缝改造量。

- T6 iOS: `BuildTarget.CAPACITOR_IOS` (与 android 一致走 capacitor, 非 tauri-mobile 实验性); macOS runner + xcodebuild 产 .ipa
- T9 鸿蒙: `BuildTarget.HARMONY_HAP` (DevEco CLT + hvigorw assembleHap); ArkTS/ArkUI 脚手架与现有 react/web 前端正交, 可能需新脚手架适配; 工具链容器化支持差, 倾向原生/专用镜像

### T4/T7/T10 — 签名对接

- T4 Windows ✅ done: SignerType.WINDOWS (signtool + EV 证书) 已存在 (v6.1), 本步**对接构建产物 + 消费 T2 真相源**。补 T2 遗漏 `BuildArtifactKind.APP` (mac .app → APPLE) + 新增 `EXTENSION_KIND` (扩展名→形态真相源); `_detect_sign_targets` 从扩展名硬编码 (.exe→WINDOWS) 改读 `EXTENSION_KIND`+`signer_for_kind` (消费 `KIND_SIGNER_TYPE`, 消除 T2 悬空), 新增 .msi 扫描→WINDOWS。现有 .app/.exe/.apk 签名零回归 (4 既有 + 1 新 msi 测试过)。代码改造独立于 T3-g (签名路由不依赖构建路径), 端到端 blocked 于 T3-g (产物) + Windows EV 证书。
- T7 iOS: 新增 SignerType.IOS (xcrun + provisioning profile + Apple Developer account), 区别于 mac .app 的 notarytool
- T10 鸿蒙: 新增 SignerType.HARMONY (华为 .p12 + .cer + .profile, hap-sign-tool 或 DevEco signing)

### T11 — 前端透出

`CreateProjectModal.tsx` BUILD_TARGET_OPTIONS 加三新目标卡片 (Windows/iOS/鸿蒙), 透出 requiresDocker 标注 (新平台走 CI runner, 标注与容器构建不同的部署形态提示)。`types/api.ts` BuildTarget 类型同步。

## 关键路径

```
关键路径: T1 → T2 → T3 → T4 = Windows 波次 (决定整体架构 + 第一个平台闭环)
并行路径: iOS 波次 (T5→T6→T7), 鸿蒙波次 (T8→T9→T10) — 依赖 T1 后可并行, 但按"依次"原则顺序推
归档前置: T11 (前端) + T12 (质检) 收尾
```

## 对后续版本的约束

- 延续 v6.18: import 环治理用契约/工厂分离; .env.example dict 字段禁止裸空值
- 延续 v6.17: skill 取数走 CapabilityProvider.load_phase_skills; ToolSpec 唯一表达
- 延续 v6.16: process_constraint 单一真相源; gate 阈值读 GateProfile; 测试 savepoint 隔离
- 延续 v6.15: DAG 三档共享硬不变量; ProcessConfig 仅持 constraint
- **v6.19 新增**: 新增 BuildTarget 必须同步 12 处注册点 (禁止只改枚举); **且必须同步 `domain/sandbox/execution_backend.py` TARGET_BACKENDS 登记执行后端 (DOCKER/CI), 未登记则 `target_execution_backend` 抛 ValueError (T1 立的单一真相源)**; 新平台先 artifact 显式建模再接构建 runtime (禁止跳层); SignerType 新增类型 (IOS/HARMONY) 必须配套签名器实现 + 凭证加密存储 (延续 v6.1 Fernet); **新平台构建走 CI 编排 (方案A), 不扩 SandboxRuntime ABC** (契约错配, 见 T1 决策)

## 遗留与环境依赖 (blocked 项, 解除前可做 artifact 建模/注册, 不可端到端)

- Windows: 需 Windows CI runner (GHA windows-latest) + Windows EV 代码签名证书
- iOS: 需 macOS CI runner (GHA macos-latest) + Apple Developer 账号 + iOS provisioning profile
- 鸿蒙: 需 DevEco Command Line Tools 环境 + 华为开发者证书
- 端到端真实验证待上述环境/凭证就绪; artifact 建模 + 构建目标注册 + 签名器实现可在 blocked 期用 mock 推进 (延续 v6.1 mock 验证签名链路模式)

> 教训记录 (v6.18 归档): 质检须区分 "arc Settings 字段对齐" 与 "compose infra 变量", 勿将 infra 变量误判为配置缺失阻断项 (第五次假阳性, 见 [[feedback-archive-quality-grep-variants]])。

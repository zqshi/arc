# v6.19.0 Current — 方向待定

> 状态: 已激活 — v6.18 归档后激活, 恢复"有且仅有一个 *-current.md"协议不变量。2026-06-29: v6.18 投产治理升级已归档 (见 [v6.18.0-snapshot.md](v6.18.0-snapshot.md))。
> 上一版本: [v6.18.0](v6.18.0-snapshot.md)
> 上下文: v6.18 消除投产阻断项 (lint/import环/配置对齐/仓库卫生/镜像验证) + 固化验证闭环。下一版本可承接 backlog pending 技术债务, 或定产品方向, 待用户决策。

## 候选

- backlog pending 技术债务 (infra 变量前缀重构 P2 / application 层 service 覆盖度评估 / 聚合边界重构 / 前端测试体系等, 见 backlog.md 技术债务表)
- v6.18 遗留: OpenHands/Codex MCP 真注入扩展 (仍 blocked 于 runtime 环境: OpenHands localhost:3000 未启动 / Codex api_key 未配) — 解除 blocked 后可承接
- 产品方向待定 (AI 驱动第二代演化 / 新项目类型 / 商业化路径, 见 docs/arc-product-vision.md)

## 待办（新会话接手）

> v6.19 方向为阻塞项, 需用户决策。定方向后据此建任务依赖表, current.md 从"方向待定"转正常版本。

1. **v6.19 方向定夺（阻塞项, 需用户决策）** — 见候选区三选项。定方向后建任务依赖表。

> 教训记录 (v6.18 归档): 质检须区分 "arc Settings 字段对齐" 与 "compose infra 变量", 勿将 infra 变量误判为配置缺失阻断项 (v6.18 归档第五次假阳性, 见 snapshot 教训记录)。

## 对后续版本的约束

- 延续 v6.18: import 环治理用契约/工厂分离模式 (禁止再引入 application↔infrastructure 直接环); .env.example dict 字段禁止裸空值, 留空用注释
- 延续 v6.17: skill 取数统一走 `CapabilityProvider.load_phase_skills`; skill 工具集 `ToolSpec` 唯一表达 (source=inline|mcp); `CapabilityType.MCP` 已实现 (非预留)
- 延续 v6.16: `process_constraint` 单一真相源; gate 阈值读 `GateProfile`; 测试 savepoint 隔离
- 延续 v6.15: DAG 三档共享硬不变量; `ProcessConfig` 仅持 constraint

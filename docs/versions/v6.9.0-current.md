# v6.9.0 Current — test_health + artifact + 按类型编排 + 能力升级

> 状态: 进行中 (后端核心交付, 前端体验续会话)
> 起始 commit: 3aa6ec5 (2026-06-26)
> 上一版本: [v6.8.0](v6.8.0-snapshot.md)
> 分支: feat/v6.9-test-health-fix (基于 feat/v6.8)
> 计划: ~/.claude/plans/zesty-swimming-noodle.md (编排 A+C+D, B分离)

## 已完成 commits (feat/v6.9-test-health-fix)

| commit | 内容 |
|--------|------|
| 1ec7d2e | test_health 时序污染修复(方案E conftest monkeypatch) |
| 11f259e | artifact ①② BUILD artifact domain + service 锚点 |
| 03ff99d | artifact ③ builder 接入(extractor 自动抽 BUILD) |
| 94169c5 | artifact ④核心 _resolve_build_status 读 BUILD |
| 004c419 | A1+A2b 按类型编排 — DELIVERABLES_BY_TYPE + app_code 类型过滤 |
| bb98df0 | A2a tracker.required 按类型裁剪(不显示 app_code 节点) |
| a1a2c2e | C1 SkillLoader 多来源(inline 内联 skill) |

## 工作流进展

### test_health (done, 待CI)
- 方案E conftest monkeypatch 全局 factory 隔离, 否决方案A(Depends破坏/health语义)
- T4 CI待验证(v6.8合main后开PR); T5 opensandbox本地缺包建议 pip install -e .

### artifact 显式建模 (①②③④核心 done, ⑤前端续)
- BUILD artifact domain/service/extractor产出/消费侧读BUILD, 双读兼容
- ⑤前端BUILD展示 + ④其余消费点(_deploy_project/build_gate/_scan_products/签名)续会话

### A 按类型编排流程 (核心 done, 用户核心诉求)
- A1 domain: DELIVERABLES_BY_TYPE/PHASES_BY_TYPE + is_deliverable_visible(复用charter注册表模式)
- A2b artifact_extractor: 非app类不产出 app_code(_get_project_type graceful)
- A2a conversation_strategy: tracker.required 按类型裁剪(前端不显示 app_code 节点)
- A3 enabled_phases手动端点: 方向不一致(用户要类型自动非手动)缓做
- A4 artifact④⑤: ④核心done, ⑤并入前端

### C 能力管理升级 (C1 done, C2/C3续)
- C1 SkillLoader 多来源: directory(现有)+inline(直接填content, 无需SKILL.md文件) ✅
- C2 capability.config schema值对象: C1已让source/content生效, 强制schema缓做(改Capability值对象风险)
- C3 前端CapabilityEditorModal升级: source切换+inline编辑+上传+skill-creator+agent分组 续会话

### D Agent 显性化管理 (后端v6.8已覆盖, 前端续)
- D1 后端: agent声明复用capability表(type=AGENT, v6.8 W2.1统一), /api/capabilities CRUD已有, 无需新做
- D2 前端AgentManager: agent在CapabilityManager已管理(type分组), 专属视图续会话

## 分离版本 (B, v6.10候选)
- 环节逻辑内容编排(methodology/prompt/gate可编排, 流程引擎重构) — 高风险独立版本

## 续会话接续点

### 前端体验 (C3 + D2 + artifact⑤, 一起做更高效)
- CapabilityEditorModal: skill type 时 source切换(directory/inline) + inline content textarea, 替代纯JSON
- skill 文件上传组件(→storage, 需storage读方法) + skill-creator 引导入口
- agent 专属管理视图(CapabilityManager内type=agent分组强化)
- artifact BUILD 展示(只app类项目)
- 前端必须 tsc --noEmit + vitest(memory警示tsc漏拦)

### 后端剩余
- artifact ④其余消费点按类型分流(_deploy_project/build_gate/_scan_products/签名 双读兼容已建)
- A2前端阶段UI按enabled_phases裁剪(PHASES_BY_TYPE两类型都全, 当前无差异, 后续类型激活时)

## 归档条件
后端核心 + 前端体验完成 + 6.1-6.7必修项 → snapshot → 激活下一版本

## 约束
- 延续v6.8约束; 双读兼容期不破坏存量; 前端tsc必跑
- 新会话开始: 读本文件 + 执行会话级巡检

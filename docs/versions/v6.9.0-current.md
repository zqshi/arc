# v6.9.0 Current — test_health 修复 + artifact 显式建模

> 状态: 进行中 (test_health 已提交待 CI, artifact ①②done ③推进中)
> 起始 commit: 3aa6ec5 (2026-06-26)
> 上一版本: [v6.8.0](v6.8.0-snapshot.md)
> 来源: backlog P2 🔴 test_health + 用户"依次进行/全量自循环" → artifact 显式建模

## 工作流 A: test_health 时序污染修复

| ID | 任务 | 状态 |
|----|------|------|
| T1 | 复现 degraded(本地 opensandbox 缺包受阻, 基于 backlog 🔴+代码分析) | done |
| T2 | /health 隔离全局 engine(方案 E conftest monkeypatch) | done |
| T3 | 补 degraded 路径测试 | done |
| T4 | CI 全量验证(待 v6.8 合 main 后开 PR) | partial |
| T5 | (计划外) opensandbox 本地缺包, 建议 pip install -e . | pending |

**提交**: 1ec7d2e (feat/v6.9-test-health-fix 分支, 基于 feat/v6.8)
**方案 E**: conftest client fixture monkeypatch 全局 async_session_factory 复用 db_session 独立 engine, /health 与全局 engine 跨 loop 污染解耦。否决方案 A(Depends 破坏 /health 总返回 200 语义)

## 工作流 B: artifact 显式建模 — 构建产物 BUILD artifact

> 计划: ~/.claude/plans/zesty-swimming-noodle.md (全量①-⑤)
> 断层: 构建产物(dist/apk/binary)散在 prototype content(build_status/artifact_path) + deploy_report.build_evidence, 无统一 artifact 锚点
> 利好: ArtifactModel 通用(artifact_type String + content JSONB), 新增 BUILD 无需 migration

| ID | 波次 | 状态 | 风险 |
|----|------|------|------|
| W1 | ①domain建模(BUILD枚举/字段/policy/map/labels) + 单测 | done | 低 |
| W2 | ②ArtifactService(create_or_update_build/update_build_status) + 单测 | done | 低 |
| W3 | ③builder接入(extractor DEVELOPMENT 产出 BUILD, BINARY_APP→tauri_linux) | in_progress | 高(改 artifact_extractor 622行警告区) |
| W4 | ④消费改造+迁移(deployer/hooks/build_gate/签名/分发读 BUILD, 双读兼容) | pending | 高 |
| W5 | ⑤前端 BUILD 展示 | pending | 中 |

**关键发现(③调研)**: 当前仅 BINARY_APP→TAURI_LINUX 构建链路激活(build_images.py:24)。STATIC_SITE 走 dist 静态站点部署无 build_target/签名/分发。故 BUILD artifact 主要服务 BINARY_APP, STATIC_SITE 暂不产出 BUILD(仍走 prototype content)。

## 归档条件

test_health T4 CI 验证 + artifact 全量①-⑤(或本期①②③) + 6.1-6.7 必修项 → snapshot → 激活下一版本

## 约束

- 延续 v6.8 约束(见 [v6.8.0-snapshot.md](v6.8.0-snapshot.md))
- artifact: 仅 domain+service 改业务代码(extractor③谨慎, 不超 800 行); 双读兼容期避免破坏存量
- 新会话开始: 读本文件 + 执行会话级巡检

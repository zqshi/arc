# v6.9.0 Current — test_health 修复 + artifact 显式建模

> 状态: 进行中 (test_health + artifact ①②③④核心 已提交, ④其余 + ⑤ 续会话)
> 起始 commit: 3aa6ec5 (2026-06-26)
> 上一版本: [v6.8.0](v6.8.0-snapshot.md)
> 分支: feat/v6.9-test-health-fix (基于 feat/v6.8, 待 v6.8 合 main 后开 PR)
> 计划: ~/.claude/plans/zesty-swimming-noodle.md (artifact 全量①-⑤)

## 已完成 commits (feat/v6.9-test-health-fix)

| commit | 内容 |
|--------|------|
| 1ec7d2e | test_health 时序污染修复(方案E conftest monkeypatch 全局 factory 隔离) |
| 11f259e | artifact ①② BUILD artifact domain + service 锚点 |
| 03ff99d | artifact ③ builder 接入(extractor _try_produce_build_artifact, 方案B自动抽) |
| 94169c5 | artifact ④核心 _resolve_build_status 读 BUILD fallback |

## 工作流 A: test_health 时序污染修复 (done, 待 CI)

- T1-T3 done: 复现(本地opensandbox缺包受阻,基于backlog🔴+代码分析) / 方案E / degraded测试
- T4 CI待验证: v6.8 合 main 后 v6.9 开 PR 跑 CI(unit全量后 test_health ok)
- T5(计划外): opensandbox>=0.1.0 pyproject必装本地未装, 阻塞本地unit全量, 建议 `pip install -e .`
- **方案E**: conftest client fixture monkeypatch 全局 async_session_factory 复用 db_session 独立 engine, /health 与全局 engine 跨 loop 污染解耦。否决方案A(Depends破坏/health总返回200语义)

## 工作流 B: artifact 显式建模 — 构建产物 BUILD artifact

> 断层: 构建产物(dist/apk/binary)散在 prototype content(build_status/artifact_path) + deploy_report.build_evidence, 无统一 artifact 锚点
> 利好: ArtifactModel 通用(artifact_type String + content JSONB), 新增 BUILD 无需 migration

| 波次 | 状态 | 要点 |
|------|------|------|
| ①domain建模 | ✅ done | ArtifactType.BUILD + PHASE_ARTIFACT_MAP[DEVELOPMENT] + DELIVERABLE_REQUIRED_FIELDS + ARTIFACT_LABELS + policy只读 |
| ②ArtifactService | ✅ done | create_or_update_build + update_build_status + _find_build_artifact |
| ③builder接入 | ✅ done | extractor._try_produce_build_artifact(方案B自动抽, BINARY_APP→tauri_linux, STATIC_SITE/无build_status跳过) + build schema |
| ④消费改造+迁移 | ⏳ partial | 核心 _resolve_build_status 读BUILD fallback✓; 其余 pending(见下) |
| ⑤前端 | ⏳ pending | 续会话 |

### ④续会话要点(双读兼容期, prototype content 仍可读, 逐步迁移)

- `_deploy_project`(artifact_deployer.py:135): 读 BUILD.artifact_path fallback prototype.content(project_dir+artifact_path)
- `build_gate`(build_gate.py): check_build_ready 读 BUILD.build_status
- `DeployService._scan_products`(deployment/service.py:199): 从 BUILD.product_path 取产物(替代扫描目录按后缀)
- `Signer.sign`(domain/deployment/signer.py:112): 取 BUILD.product_path
- 迁移: prototype content build_status/artifact_path → BUILD artifact(④核心已 fallback, 存量不破)
- **关键**: 双读兼容(BUILD不存在 fallback prototype), 避免破坏存量项目

## 归档条件

test_health T4 CI验证 + artifact ④⑤完成 + 6.1-6.7 必修项 → snapshot → 激活下一版本

## 约束

- 延续 v6.8 约束(见 [v6.8.0-snapshot.md](v6.8.0-snapshot.md))
- artifact: 双读兼容期, prototype content 仍可读, ④其余逐步迁移不破坏存量
- 新会话开始: 读本文件 + 执行会话级巡检(DDD层级/循环依赖/文件超限)

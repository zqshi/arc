# v6.9.0 Current — 方向待定（待规划）

> 状态: 进行中 (v6.8.0 归档后激活, 方向待用户选择)
> 起始 commit: 30224e5 (2026-06-26)
> 上一版本: [v6.8.0](v6.8.0-snapshot.md)
> 来源: v6.8 归档后 backlog 无 v6.9 明确规划, 方向待定

## 版本目标

待定。v6.8.0 能力注册表全任务 done 后, 无自然驱动版本。候选方向见下, 待用户选择其一或自定义。

## 候选方向（待用户选择其一或自定义）

1. **T4 project_member repository 接口补齐** — v6.6/v6.7 遗留 P2。需先定 project_member 归属聚合(project 还是 organization), 再补 AbstractRepository + infrastructure 实现。设计决策非纯机械
2. **v6.0 波次2/3 构建链路** — backlog P2。激活 BuildTarget.WEB(arc/web-builder 镜像) + BuildTarget.CAPACITOR_APK(arc/android-builder 镜像), runtime 后端延续
3. **artifact 显式建模** — BaaS/容器 runtime 接入前置(记忆: 先补 artifact 显式建模再接 runtime, 跳层会被劝退)
4. **oss-evaluator 技术选型评估接入** — backlog P2, 待 web search tool
5. **能力运行时热加载 / agent 环节级选配** — v6.8 遗留。SKILL.md 变更热重载 + agent 能力环节级调度(本期仅注入提示文本)
6. **test_health 全量连跑时序污染修复** — backlog P2。/health 端点用全局 session_factory 不走 db_session fixture, unit 全量跑完后连接池异常终止返回 degraded, 测试基础设施隔离缺陷
7. **其他用户指定方向**

## 归档条件

方向定 + 任务完成 + 6.1-6.7 必修项全过 → snapshot → 激活下一版本

## 约束

- 延续 v6.8 约束(见 [v6.8.0-snapshot.md](v6.8.0-snapshot.md))
- 新会话开始: 读本文件 + 执行会话级巡检(DDD 层级/循环依赖/文件超限)

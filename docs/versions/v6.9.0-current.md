# v6.9.0 Current — test_health + artifact + 按类型编排 + 能力升级

> 状态: 后端核心已整合 main, 前端体验待续(新会话从 main 切分支)
> 起始 commit: 3aa6ec5 (2026-06-26)
> 上一版本: [v6.8.0](v6.8.0-snapshot.md)
> main 整合: d023b08(Merge v6.8) + c402f7c(Merge v6.9), feature 分支已全清理
> 计划: ~/.claude/plans/zesty-swimming-noodle.md (编排 A+C+D, B分离)

## main 整合记录 (2026-06-27)

feat/v6.8 + feat/v6.9 --no-ff 合并进 main 并推送远端, feature 分支(含旧 v6.3/v6.7 残留)全部清理。仓库仅剩 main。

| merge | 内容 |
|--------|------|
| d023b08 | Merge feat/v6.8: 能力注册表 — Agent/Skill 声明管理 + 环节级配置 |
| c402f7c | Merge feat/v6.9: test_health修复 + artifact显式建模 + 按类型编排 + 能力升级 |

### 已交付后端核心 (v6.9 commits)
| commit | 内容 |
|--------|------|
| 1ec7d2e | test_health 时序污染修复(方案E conftest monkeypatch) |
| 11f259e | artifact ①② BUILD artifact domain + service 锚点 |
| 03ff99d | artifact ③ builder 接入(extractor 自动抽 BUILD) |
| 94169c5 | artifact ④核心 _resolve_build_status 读 BUILD |
| 004c419 | A1+A2b 按类型编排 — DELIVERABLES_BY_TYPE + app_code 类型过滤 |
| bb98df0 | A2a tracker.required 按类型裁剪(不显示 app_code 节点) |
| a1a2c2e | C1 SkillLoader 多来源(inline 内联 skill) |

## 新会话待办 (从 main 切新分支继续)

### 1. 前端体验 (C3 + D2 + artifact⑤, 一起做更高效) — 主待办
- **C3 CapabilityEditorModal 升级**: skill type 时 source 切换(directory/inline) + inline content textarea, 替代纯 JSON textarea; skill 文件上传组件(→storage, 需加 storage 读方法) + skill-creator 引导入口
- **D2 AgentManager**: CapabilityManager 内 type=agent 分组强化(专属视图), agent 声明后端 v6.8 已覆盖(/api/capabilities type=AGENT)
- **artifact ⑤ BUILD 展示**: 只 app 类项目显示(复用 PHASES_BY_TYPE/DELIVERABLES_BY_TYPE)
- ⚠️ **前端必跑 tsc --noEmit + vitest**(memory 警示 tsc 漏拦曾发生, 不能只靠 Vite dev)

### 2. test_health T4 CI 验证
- main 已含修复(1ec7d2e), CI 跑 unit 全量后 test_health 应 ok
- 本地 opensandbox 缺包(T5): `pip install -e .` 同步后才能本地跑 unit 全量复现

### 3. artifact ④其余消费点按类型分流
- _deploy_project(artifact_deployer.py:135) / build_gate / DeployService._scan_products(deployment/service.py:199) / Signer.sign(signer.py:112)
- 双读兼容已建(④核心 _resolve_build_status), 按类型分流: app 类走 BUILD, static_site 走原 prototype content

### 4. A2 前端阶段 UI 按 enabled_phases 裁剪
- PHASES_BY_TYPE 两类型当前都全7阶段, 无差异; 后续激活新 ProjectType(如 backend_service)时按类型裁剪阶段显示

## 工作流进展(后端)

### A 按类型编排流程 (核心 done, 用户核心诉求)
- A1 domain: DELIVERABLES_BY_TYPE/PHASES_BY_TYPE + is_deliverable_visible(复用 charter 注册表模式)
- A2b artifact_extractor: 非app类不产出 app_code(_get_project_type graceful)
- A2a conversation_strategy: tracker.required 按类型裁剪(前端不显示 app_code 节点)
- A3 enabled_phases 手动端点: 方向不一致(用户要类型自动非手动)缓做

### C 能力管理升级 (C1 done, C2/C3续)
- C1 SkillLoader 多来源: directory(现有)+inline(直接填 content, 无需 SKILL.md 文件) ✅
- C2 capability.config schema 值对象: C1 已让 source/content 生效, 强制 schema 缓做
- C3 前端升级: 续(见待办1)

### D Agent 显性化管理 (后端 v6.8 已覆盖, 前端续)
- D1 后端: agent 声明复用 capability 表(type=AGENT, v6.8 W2.1 统一), /api/capabilities CRUD 已有
- D2 前端 AgentManager: 续(见待办1)

## 分离版本 (B, v6.10 候选)
- 环节逻辑内容编排(methodology/prompt/gate 可编排, 流程引擎重构) — 高风险独立版本

## 归档条件
前端体验 + ④其余 + CI 验证完成 + 6.1-6.7 必修项 → snapshot → 激活下一版本

## 约束
- 延续 v6.8 约束; 双读兼容期不破坏存量; 前端 tsc 必跑
- 新会话开始: 读本文件 + 执行会话级巡检(DDD 层级/循环依赖/文件超限)

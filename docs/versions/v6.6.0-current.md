# v6.6.0 Current — 代码质量修复 (收尾, 待归档)

> 状态: 主体完成 (10 commits 已合并 main, 49d7260)。仅剩 P3 记录项, 新会话可选择性推进后归档
> 起始 commit: b1c020a (v6.5 归档后)
> 最新 commit: 49d7260 (main, 已 push origin)
> 上一版本: [v6.5.0](v6.5.0-snapshot.md)
> 来源: 用户要求全项目深度质量审计 + 全修含重构

## 版本目标

基于全项目深度审计(后端545文件+前端117文件), 修复死代码/冗余/逻辑泄漏/接口漂移/方法膨胀/配置不一致。TDD 路径(补单测→拆分→行为保持)验证可行, 累计修 2 真实 latent bug。

## 已完成 (10 commits, 全合并 main)

### P1 架构清理
- 删后端死代码 `experience/analytics.py` (218行零引用) + 前端6死代码文件 + inspector孤儿
- k8s 占位符 `:placeholder` → `:latest`; 移除冗余依赖 `websockets`

### P2 route逻辑泄漏抽service (+32单测)
- 新建 SettingsService/TemplateService, 扩展 ExperienceService(create/update), 3处route降至参数校验+调用

### P2 repository接口漂移补齐
- domain 补 AbstractUserRepository + AbstractOrganization(Member)Repository, infrastructure继承

### P2 前端清理 + 重构
- Field重复合并→shared.LabeledField; vite esbuild.drop; globPatterns移除png
- ProjectDetail抽SuggestionsPanel(405→329); TodosTab props聚合32→15(+4组件单测)
- useConversationSocket onmessage 180行switch→分发器+4子函数(全<80行, +14单测)

### P2 service方法拆分 (TDD, 4个超80行全拆, 修2真实bug)
- deploy(108→45) / extract_from_todo(112→36) / confirm_phase(100→33, 修GateResult UnboundLocalError) / execute(102→49, 修ToolLoopEvent NameError)

### P3: mcp _call_tool if分发改字典表

## 待办 (新会话接续, 按价值排序)

### T1 [建议做] ExperienceInjectionLog 孤儿表清理
- 背景: analytics.py 删除后, `infrastructure/models/experience.py` 的 `ExperienceInjectionLog` model + migration `z7_experience_injection_logs` 成孤儿(grep 确认仅 migration 互相引用)
- 步骤: 1) grep 确认 model 零业务引用 2) 新增 alembic migration drop table (downgrade 保留建表) 3) 删 model 类 4) 跑 migration 验证
- 验收: ruff 0 + pytest 全绿 + migration up/down 双向可执行
- 风险: 低 (表无业务写入, analytics 已删)

### T2 [建议做] artifact-renderers lazy import 统一
- 背景: `artifact-renderers/index.tsx` 多数 renderer 顶层 import, 唯 MermaidDiagram 内部 lazy — 风格不一致且首屏加载未用 renderer
- 步骤: 1) index.tsx 全改 `lazy()` + 包 `Suspense` fallback 2) 确认调用方(UnifiedWorkspaceView 等)有 Suspense 边界 3) build 验证 chunk 分割
- 验收: tsc -b 0 + build通过 + vitest 全绿 + 首屏 chunk 体积下降
- 风险: 中 (需 Suspense 边界, 漏包会运行时报错)

### T3 [可选] GitHubSection 19 props 聚合
- 背景: TodosTab 已聚合验证模式, GitHubSection 同类问题但主组件薄分发(逻辑在 ConnectedView/DisconnectedView 子视图, Omit/Pick 派生类型)
- 步骤: 复用 TodosTab 模式聚合为 gitConfig/gitActions 子对象 + 补组件单测
- 验收: tsc + vitest 行为保持
- 风险: 低 (模式已验证) | 收益: 低 (薄分发, 聚合后子视图类型派生反而可能更复杂, 需评估)

### T4 [可选] project_member repository 接口
- 背景: domain 无 project_member 模块, `infrastructure/repositories/project_member.py` 的 `MemberInfo` DTO 归属未定
- 步骤: 1) 先定聚合边界(project_member 属 project 还是 organization 聚合) 2) domain 建 project_member 模块或归入 project 3) 补 AbstractRepository + infrastructure继承
- 验收: DDD 分层合规 + ruff 0
- 风险: 中 (需先定聚合边界, 设计决策非纯机械)

## 明确不做 (评估结论, 不再列入待办)

- **聚合边界收敛** (pipeline 持4聚合repo): 编排服务多聚合协调是 DDD application service 合理模式, 强收敛需造薄wrapper属形式主义, 非实质改善
- **execution 3文件 500-800行** (execution_engine 551/artifact_extractor 546/tool_loop 509): 遵循 v6.5 "评估保持"先例, 单方法未超80行(文件级超限非方法级), 待超800或明确动机再拆
- **useConversationSocket 文件523行**: 单函数已全<80行强制阈值, 文件级超限源于hook天然复杂度(18事件+连接/重连/token刷新), 拆分会割裂内聚逻辑

## 归档条件

T1-T4 任选完成后(或全部跳过), 执行版本完成质量检测协议(CLAUDE.md 第6章 6.1-6.7):
- 6.1-6.5/6.7 必修项全过 → current.md 精简为 snapshot → 重命名 v6.6.0-snapshot.md → 激活 v6.7-current.md(从 backlog 取或新建)

## 约束

- 保持 CI 绿色 (ruff 0 + tsc -b 0 + build通过 + 1800后端unit测试 + 59前端测试)
- 提交前必须跑 `npm run build` (tsc -b), `tsc --noEmit` 拦不住 unused import
- TDD: 高风险重构先补 characterization 单测再改, 测试网保护下推进
- 新会话开始: 读本文件 + 执行会话级巡检(DDD层级/循环依赖/文件超限)

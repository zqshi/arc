# v6.6.0 Current — 代码质量修复

> 状态: 进行中 (质量审计修复, P1+P2 大部分完成)
> 起始 commit: b1c020a (v6.5 归档后)
> 上一版本: [v6.5.0](v6.5.0-snapshot.md)
> 来源: 用户要求全项目深度质量审计 + 全修含重构

## 版本目标

基于全项目深度审计(后端545文件+前端117文件), 修复死代码/冗余/逻辑泄漏/接口漂移/方法膨胀/配置不一致。保留有测试网的高价值重构, 无测试网的高风险项记入技术债务待补测试后做。

## 已完成 (本次会话)

### P1 架构清理
- 删后端死代码 `experience/analytics.py` (218行零引用)
- 删前端6死代码文件: MembersTab/SmartActionBar/QuickPrompts/TemplateList/TemplateCard/InteractivePrototype + inspector.ts(孤儿) + barrel清理
- k8s 占位符 `arc-*-:placeholder` → `:latest` (与 docker-compose.prod 一致)
- 移除冗余依赖 `websockets` (src 零使用, uvicorn[standard] 间接带入)
- 修正 ChatMessages 误导注释 (InteractivePrototype 未接线半成品)

### P2 route层逻辑泄漏抽service (+32单测)
- 新建 `application/settings/service.py` (SettingsService: 运行时覆盖+.env持久化+adapter缓存失效)
- 扩展 ExperienceService (`create`/`update` + `_generate_embedding`/`_apply_updates` 复用, 修正 tags model_dump dict 访问 bug)
- 新建 `application/template/service.py` (TemplateService: update+confirm+publish+deprecate)
- 3处route降至仅参数校验+调用

### P2 repository接口漂移补齐
- `domain/user/repository.py` (AbstractUserRepository) + infrastructure 继承
- `domain/organization/repository.py` (AbstractOrganization+MemberRepository) + infrastructure 继承
- 消除12个有接口模块中3个缺口的2个, 与项目模式对齐

### P2 前端清理
- Field重复合并: AppCode.tsx+ServiceSpec.tsx → shared.tsx `LabeledField`
- vite.config 加 `esbuild.drop` (生产剔除console/debugger, dev保留)
- vite.config globPatterns 移除无效png

### P2 service方法拆分 (TDD: 补单测→拆分, 顺带修2真实bug)
- deployment `deploy` (108行) → 拆 `_execute_deploy_steps`+`_handle_deploy_failure`, deploy降至45行 (e2e测试保护)
- experience `extract_from_todo` (112行) → 拆 `_already_extracted`+`_build_conversation_log`+`_extract_experience_data`+`_build_experience`, 主方法降至36行 (补5单测后拆分, 行为保持)
- pipeline `confirm_phase` (100行) → 拆 `_evaluate_phase_gate`+`_confirm_and_advance`+`_trigger_phase_side_effects`+`_advance_to_next`+`_complete_pipeline`, 主方法降至33行 (补10单测后拆分)
  - **修bug**: GateResult 仅在 `if not artifact` 块内局部import, DEPLOYMENT阶段build未就绪时(BuildGateError路径)→UnboundLocalError 而非 PhaseGateError
- orchestration `execute` (102行) → 拆 `_execute_layers`(async gen, list累积worker_results)+`_build_plan_summary`, 主方法降至49行 (补4单测后拆分)
  - **修bug**: ToolLoopEvent 仅 TYPE_CHECKING import 无运行时import, multi-agent路径 `yield ToolLoopEvent(...)` → NameError (single-agent fallback 因不构造事件而幸存, multi-agent完全broken)

### P2 前端重构 (低风险部分 + TDD)
- ProjectDetail 抽 `SuggestionsPanel` 独立组件 (405→329行)
- TodosTab props 聚合: 32 props → 15 (versionForm/versionActions/todoActions 3子对象 + 12其他), 不改useProjectDetail影响面小 (补4组件单测后重构, tsc+vitest验证行为保持)
- useConversationSocket onmessage 拆分: 475行 hook 的 onmessage 180行 switch → 分发器 handleEvent(33行) + 4子函数 handleMessageEvent(37)/handleStreamEvent(64)/handleToolEvent(44)/handleOrchestrationEvent(47), 全部<80行强制阈值 (补14单测 mock WebSocket基建后拆分, 行为保持)

### P3 优化
- mcp.py `_call_tool` 长串if分发改 `_TOOL_HANDLERS` 字典表

## 遗留技术债务

### P2 (下版本补测试后优先)
- **前端props钻孔**: TodosTab 32 props 已聚合为15 (versionForm/versionActions/todoActions); GitHubSection 19 props 同类问题待做 (低优先, 已验证模式可复用)
- **service方法拆分**: 4个超80行方法已全拆 (deploy/extract_from_todo/confirm_phase/execute), TDD补单测后拆分, 顺带修2真实bug
- **聚合边界收敛**: pipeline持4聚合repo+hooks跨聚合 — 编排服务多聚合协调是合理模式, 强收敛需造薄wrapper(形式主义), 待明确动机

### P3 (记录)
- execution 3文件 500-800行 (execution_engine 551/artifact_extractor 546/tool_loop 509): 遵循v6.5评估保持, 待超800再拆
- artifact-renderers lazy import 统一: 需Suspense边界, 中风险
- project_member repository接口: domain无project_member模块+MemberInfo DTO归属未定, 需先定聚合边界
- experience/service.py 477行: 接近500, 下版本观察
- ExperienceInjectionLog model+表: analytics.py删除后成孤儿, migration z7已建表, 待确认是否清理

## 约束

- 保持 CI 绿色 (ruff 0 + tsc -b 0 + build通过 + 1800后端unit测试 + 45前端测试)
- 提交前必须跑 `npm run build` (tsc -b), `tsc --noEmit` 拦不住 unused import (noUnusedLocals 仅 build mode 报)
- 无测试网的高风险重构记债务, 不盲目重构核心代码

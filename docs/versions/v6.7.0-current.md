# v6.7.0 Current — 方向待定（待规划）

> 状态: v6.7.0 已发版(2026-06-26, tag v6.7.0 + GHCR 镜像构建触发) — 当前进度: OpenSandbox云沙箱多worker + 对话执行双轨统一 + 质量基线修复。版本继续滚动: T2凭证写入待接, 候选方向待定, 后续 v6.7.x
> 起始 commit: 待定
> 上一版本: [v6.6.0](v6.6.0-snapshot.md)
> 来源: v6.6 归档后 backlog 无 v6.7 规划, 无自然驱动版本

## 版本目标

待定。v6.6 收尾后 CI 全绿(ruff 0 / tsc -b 0 / unit 1812 / vitest 63), 无自然驱动版本, 方向待用户选择。本会话质量审计已先修复 2 项基线必修(见下「版本启动前基线修复」)。

## 版本启动前基线修复（已完成 · 本会话）

质量审计发现 2 项 P1 必修, 已修复并验证:

- **T0a** k8s 部署文档断裂 — `k8s/kustomization.yml` 引用 `secrets.yml`(被 .gitignore 忽略, 仓库仅留 `secrets.example.yml` 模板, 属 k8s secret 标准姿势), 但 README 无任何 K8s 部署前置说明, 新人 `kubectl apply -k` 必失败。修复: README 新增「Kubernetes 部署」小节, 一次覆盖 secrets 复制 + YOUR_ORG 占位替换 + preview static dir 三项
- **T0b** 前端死代码 — `frontend/src/api/client/templates.ts` 导出 8 方法全量零调用(后端 `routes/template.py` 已就绪但前端从未接入), 按 YAGNI 删除 + 清理 `index.ts` barrel。验证: tsc exit 0 / vitest 63 passed

## 候选方向（待用户选择其一或自定义）

1. **T4 project_member repository 接口补齐** — v6.6 遗留 P2。需先定 project_member 归属聚合(project 还是 organization), 再补 AbstractRepository + infrastructure 实现。设计决策非纯机械
2. **v6.0 波次 2/3 构建链路** — backlog P2。激活 BuildTarget.WEB(arc/web-builder 镜像) + BuildTarget.CAPACITOR_APK(arc/android-builder 镜像), runtime 后端延续
3. **artifact 显式建模** — BaaS/容器 runtime 接入的前置(记忆: 先补 artifact 显式建模再接 runtime, 跳层会被劝退)
4. **oss-evaluator 技术选型评估接入** — backlog P2, 待 web search tool
5. **对话执行双轨能力对齐** [P1 · 本次审计发现] — `ConversationService`(pipeline 模式)自实现 `_tool_aware_stream`, 缺沙箱隔离/多 worker 编排/artifact 抽取三项; `ConversationExecutionService`(unified 模式)经 ExecutionEngine 全具备。需先确认 pipeline 模式是否本就不需这三项: 若需要 → 让 ConversationService 复用 ExecutionEngine; 若不需要 → 记录为设计权衡
6. **签名/分发凭证写入链路接通** [P2 · 本次审计发现] — `infrastructure/crypto.py:encrypt()` + `Project.set_signing_creds()/set_distribution_creds()` 零调用, 凭证读取解密链路(signer/distributor `load_*_creds`)已通但写入加密链路从未实现。需定凭证配置 API 是否在排期: 在排期则保留待接, 否则删除避免误导
7. **其他用户指定方向**

## 任务依赖图

```
T1 对话执行双轨统一(ConversationService 复用 ExecutionEngine) [✅ 完成]
  ├─ T1.0 调研 ✅: 比对两处 _tool_aware_stream + PromptBuilder 双路径, 确认 phase_scope legacy 是过渡遗留, 用户决策彻底统一到 ContextAssembler
  ├─ T1.1 测试 ✅: 重写 test_conversation_service.py(删6死代码测试 + 加3委托测试, TDD Red→Green)
  └─ T1.2 重构 ✅: service.py 498→135行, 委托 ExecutionEngine, 删~360行死代码(PURPOSE_TO_PHASE/_build_system_prompt/_tool_aware_stream等), routes 去重双重 add_message
  验证: ruff 0 / unit 1809(基线1812-3删死代码测试) / integration 102 / tsc 0 / vitest 63

T2 签名/分发凭证配置 API [✅ 完成 · 波次1]
  ├─ T2.0 调研 ✅: 确认读取侧字段(Apple/Win/Android签名 + AppStore/PlayStore/Tauri分发) + require_project_role(ADMIN)权限helper + model enc字段已建(无migration)
  ├─ T2.1 测试 ✅: test_deploy_credentials.py 7单元(TDD Red→Green, 零mock domain验set/get闭环+encrypt接通)
  ├─ T2.2 service ✅: DeployService加configure_signing/distribution_creds + list_credentials, 接通零调用方crypto.encrypt
  ├─ T2.3 route ✅: routes/project/credentials.py(3端点) + 3 schema + 挂载
  └─ T2.4 集成 ✅: test_project_credentials.py 5集成(CRUD+越权401+枚举422)
  验证: ruff 0 / pytest 1923(基线1911+12) / 接通写入链路, deploy签名不再恒skip
  遗留: 凭证清除能力(显式DELETE)超T2范围, 留后续增强

波次2 skill 运行时配置 API [✅ 完成]
  ├─ W2.1 registry热重载 ✅: AgentRegistry.reload()原地重建(不替换单例, session_manager顶层import透明), 抽_register_configured复用
  ├─ W2.2 schema ✅: LLMSettingsUpdate加agent adapter字段(openhands/codex/claude_code/cursor)
  ├─ W2.3 update后reload ✅: SettingsService.update涉agent字段时调agent_registry.reload
  └─ 修既有prefix bug ✅: _ENV_KEY_MAP全字段加ARC_前缀(原LLM持久化为LLM_PROVIDER与config env_prefix=ARC_不一致→配置重启丢失), agent字段同步修复
  验证: ruff 0 / pytest 1930(T2后1923+7) / skill配置env级→运行时API级

波次3 charter 遵守度门禁 [✅ 完成]
  ├─ W3.1 测试 ✅: test_conversation_gate加TestCharterCompliance(4单元, 零mock domain, 验charter注入prompt+空省略+违规降分)
  ├─ W3.2 gate ✅: evaluate_conversation_gate加charter参数 + _run_llm_review注入charter_section + GATE_EVALUATION_PROMPT加占位
  └─ W3.3 接通 ✅: artifact_extractor._validate_extracted_artifact取project charter+conventions传入(顺便接通conventions死参数), pipeline/gate传charter_section=""(pipeline模式不评charter,记技术债务)
  纠正: 架构检测第4项原判"charter驱动运行半成品"不准 — charter已注入prompt(provider.py:59-60), 真缺口是评审闭环(本波补)
  验证: ruff 0 / pytest 1934(波次2后1930+4) / charter违规可阻断推进
  遗留: pipeline模式charter评审(技术债务), charter独立合规维度(技术债务)
```

## 归档条件

方向定 + 任务完成 + 6.1-6.7 必修项全过 → snapshot → 激活 v6.8

## 约束

- 延续 v6.6 约束(见 [v6.6.0-snapshot.md](v6.6.0-snapshot.md))
- 新会话开始: 读本文件 + 执行会话级巡检(DDD 层级/循环依赖/文件超限)

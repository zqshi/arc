# v6.8.0 Current — 能力注册表: Agent/Skill 声明管理 + 环节级配置

> 状态: 进行中 (v6.7.1 发版后激活, v6.7 候选方向暂挂)
> 起始 commit: ee90d4d (2026-06-26)
> 上一版本: [v6.7.0](v6.7.0-current.md) (v6.7.1 milestone 已发, 版本未归档)

## 版本目标

补全 Arc 的"能力运行时入口": agent/skill 可声明管理 (DB 持久化), 项目按环节 (固定7阶段) 配置可用能力, 执行时按环节注入, 门禁按环节能力生成检查项。收敛自需求评审: 环节内能力可配 (非环节自定义, 保研发链路定位) + skill=SKILL.md 能力封装 + MCP 不做 (预留扩展位) + 门禁走 LLM 延伸 (非规则引擎)。

## 版本验证标准

1. 可声明管理 agent/skill 能力 (CRUD + 启用/禁用)
2. 项目可按环节配置可用能力, 执行时该环节对话注入对应能力 (skill 注入 prompt / agent 影响可用集合)
3. 门禁按环节启用能力生成检查项, 能力违规可阻断推进

## 版本范围

**做什么:**
- 波次1: 能力注册表骨架 (domain/capability + DB + CapabilityService + route)
- 波次2: agent 声明迁移 (env→DB, 双读兼容) + skill SKILL.md 加载器
- 波次3: 环节级配置 (pipeline_config.phase_capabilities) + 执行注入 + 门禁 LLM 延伸 + 前端管理页

**不做什么:**
- MCP 接入 → 预留 CapabilityType.MCP, loader 不实现, 视生态后续定
- 自定义环节 → 固定7阶段, 保研发链路定位 (v6.8 之后讨论)
- 门禁规则引擎/DSL → LLM 延伸替代
- 能力运行时热加载 → 按会话读取, 热加载记技术债务

---

## 任务依赖图

```
W1 能力注册表骨架 [进行中]
  ├─ W1.1 capability domain 值对象
  ├─ W1.2 capability model + migration (依赖 W1.1)
  └─ W1.3 CapabilityService + route (依赖 W1.2)

W2 agent 声明迁移 + skill 加载器 (依赖 W1)
  ├─ W2.1 agent env→DB 迁移 (双读兼容)
  └─ W2.2 skill SKILL.md 加载器

W3 环节配置 + 执行注入 + 门禁延伸 (依赖 W2)
  ├─ W3.1 pipeline_config.phase_capabilities + route
  ├─ W3.2 execution_engine 按环节注入
  ├─ W3.3 门禁 LLM capabilities_section
  └─ W3.4 前端管理页 + 环节配置 UI
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| W1.1 | capability domain 值对象 | P0 | - | done | `domain/capability/` (value_objects+errors+repository+__init__) + 17 单测 | 1d |
| W1.2 | capability model + migration | P0 | W1.1 | done | `infrastructure/models/capability.py` + `repositories/capability.py` + alembic z17 + 8 集成测试 | 1.5d |
| W1.3 | CapabilityService + route + schema | P0 | W1.2 | done | `application/capability/service.py` + `interface/routes/capability.py` + `schemas/capability.py` + main 挂载 + 8 route 集成 | 2d |
| W2.1 | agent env→DB 迁移 | P0 | W1 | done | `application/agent/registry.py` (声明驱动重构 env_declarations/_build_factory/rebuild + async sync_registry_from_db) + lifespan sync + 10 测试 | 2d |
| W2.2 | skill SKILL.md 加载器 | P0 | W1 | done | `application/capability/skill_loader.py` (frontmatter 解析+容错) + 8 单测 | 3d |
| W3.1 | 环节配置 + route | P1 | W2 | done | `value_objects`(phase_capabilities+VALID_PHASES) + `entity`(merge 修正+update_phase_capabilities) + `capability/service`(list_by_ids) + `operations` route(admin) + schema + `main`(DomainError→400 handler) | 1.5d |
| W3.2 | 执行按环节注入 | P1 | W3.1 | done | `context/providers/capability.py`(CapabilityProvider, 落点改走 Provider 管道) + `assembler`+`prompts`+`protocol` 接入 | 2d |
| W3.3 | 门禁 LLM 延伸 | P1 | W3.1 | done | `conversation_gate`(capabilities) + `pipeline/prompts`+`gate`(占位符) + `artifact_extractor`(_get_phase_capabilities) | 1.5d |
| W3.4 | 前端管理页 + 环节配置 | P1 | W3.1 | pending | `frontend/src/` | 4d |

## 关键路径

```
关键路径: W1 → W2.1 → W3.1 → W3.2 = ~8.5d (决定版本周期)
并行路径: W2.2 (与 W2.1 并行), W3.3/W3.4 (W3.1 后并行)
```

## 复用清单

| 复用项 | 位置 |
|--------|------|
| 注册表模式 | `infrastructure/signer/__init__.py` SIGNERS + get_signer |
| agent adapter 抽象 | `application/agent/adapter.py` CodingAgentAdapter |
| registry 热重载 | `application/agent/registry.py` reload (v6.7.1) |
| 环节配置 update | `Project.update_pipeline_config` (entity.py:103) |
| 配置查询模式 | `conversation_strategy._get_llm_config` |
| 门禁 LLM 注入 | `conversation_gate._run_llm_review` (v6.7.1 charter) |
| 权限 helper | `require_project_role(ADMIN)` (deps.py) |

## 对后续版本的约束

- CapabilityType 枚举预留 mcp, 后续 MCP 接入不改枚举只加 loader
- 环节能力配置存 pipeline_config.phase_capabilities, 固定7阶段键, 不引入自定义环节键
- agent env→DB 迁移双读期: DB 空回退 env, 保证现有项目不破

## W3 实现说明 (W3.1-W3.3 done, W3.4 前端待后续会话)

**架构落点修正**: plan 原写 W3.2 在 `execution_engine._tool_aware_stream` 注入, 代码事实是该
方法只拿现成 `llm_messages`、无 phase、不组 prompt (且文件 570 行警告区)。phase 实际在
`PromptBuilder.build_system_prompt` 推断, 经 `ContextRequest` 传 `ContextAssembler` Provider 管道。
故 W3.2 新增 `CapabilityProvider` 走 Provider 管道 (第 10 个 provider), 不动 execution_engine,
符合"复用 > 新建"。plan 详见 ~/.claude/plans/refactored-wobbling-pixel.md。

**顺带修复**:
- `update_pipeline_config` 重置式→增量深度 merge (bugfix: 部分更新不再丢 phase_capabilities 等已配字段)
- 补全局 `DomainError→400` exception handler (domain 规则违反原落 500, 现 4xx; baas/template/capability 均受益)
- `CapabilityService.list_by_ids` (W3.1 校验 + W3.2 注入 + W3.3 门禁共用)

**技术债务**:
- `artifact_extractor.py` 622 行 (警告区, 加 `_get_phase_capabilities`; <800 不必修, 后续抽 helper)
- `pipeline/gate.py` (pipeline 模式门禁) `capabilities_section` 传空, 未按环节注入 (W3.3 只延伸对话模式; pipeline 模式后续可延伸)
- agent 能力本期仅注入"可用 agent"提示文本, 运行时调度仍由 AgentRegistry (热加载/环节级选配未做)
- 能力运行时热加载未做 (SKILL.md 变更需重载, 本期按会话读取)
- type-phase 硬匹配约束未做 (skill 通用性, 改由门禁 LLM 软约束)

**验证**: ruff src All passed; pytest 2009 passed (基线 1934 +75 新增); 手动 e2e 待 W3.4 前端。

## 约束

- 延续 v6.7 约束 (见 [v6.7.0-current.md](v6.7.0-current.md))
- 新会话开始: 读本文件 + 执行会话级巡检 (DDD 层级/循环依赖/文件超限)

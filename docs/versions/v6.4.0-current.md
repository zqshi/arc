# v6.4.0 Current — 债务清理 + prompt-upgrade P2（规则残留 LLM 化）

> 状态: 进行中 (v6.3 归档后激活)
> 起始 commit: f328787 (v6.3 T1-T5 合并后)
> 上一版本: [v6.3.0](v6.3.0-snapshot.md)
> 真相源: [docs/prompt-upgrade-plan.md](../prompt-upgrade-plan.md) P2 段 + [backlog.md](backlog.md) 跨版本约束

## 版本目标

两件事收尾 Arc 的"规则执行式→意图驱动"升级与债务清理:
1. **prompt-upgrade P2 (#11-14)**: 把 execution 层 4 处残留的规则执行式判断 (正则/字面匹配/空参数/硬编码推断) LLM 化, 复用 #8-10 建立的"🟡结构预筛+🟢LLM确认+降级兜底"范式。完成后 prompt-upgrade 14/14 全清。
2. **T6 治理债务清理**: main 既有 ruff 164 (E501/F401/I001) + 前端类型 34 (TS6133/TS2322 等), 合并 v5.9-v6.2 时发现, 非任何版本引入但 CI 长期红。本版本清零让 CI 转绿。

## 版本验证标准

1. prompt-upgrade #11-14 全部 done, 14/14 完成, execution 层无规则执行式残留
2. ruff check 零 error (或仅余不可自动修且已记债务的), backend-lint CI job 转绿
3. 前端 tsc -b --noEmit 零 error, frontend-check CI job 转绿
4. 全量测试不回归 (后端 pytest + 前端 vitest)

## 版本范围

**做什么:**
- #11 architecture_methodology: 正则判事件名"过去时态" → LLM DDD 合规判断 (或保留结构校验为 🟡 预筛)
- #12 dev_test_methodology: `"FAIL" in text.upper()` 字面匹配 → LLM 解析测试结果语义
- #13 constraint_policy: `route_strategy("","",round)` 空参数残留 → 传入真实 title/description 或移除调用点
- #14 prompt_builder `_infer_phase`: 按 artifact 完成顺序硬编码推断 → LLM 推理推进/回退
- T6-ruff: 47 fixable `ruff --fix` 自动修, 余下 E501 手动折行
- T6-frontend: 16 未使用变量/import 快修 + 18 真实类型问题逐个修

**不做什么:**
- v6.0 波次2/3 (web/android 镜像) → 独立推进
- 新功能开发 → 本版本是债务清理 + 升级收尾, 不加新能力

---

## 任务依赖图

```
T1-T4 (prompt-upgrade P2 #11-14, 各自独立, 可并行)
T5 (ruff 债务清理, 独立)
T6 (前端类型债务清理, 独立)
T7 (质量检测) ─ blocked by T1-T6
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| T1 | #11 architecture_methodology LLM 化 | P0 | - | pending | `execution/architecture_methodology.py:178`, 复用 execution/llm_review.py |
| T2 | #12 dev_test_methodology LLM 化 | P0 | - | pending | `execution/dev_test_methodology.py:156`, 复用 execution/llm_review.py |
| T3 | #13 constraint_policy 空参数修复 | P0 | - | pending | `execution/constraint_policy.py:202` |
| T4 | #14 prompt_builder _infer_phase LLM 化 | P0 | - | pending | `context/prompt_builder.py:140` |
| T5 | ruff 164 债务清理 | P1 | - | done | 非architecture E501×55+F841×2 已清零; architecture 11处(E501×6+F841×2+N806×3)留T1 |
| T6 | 前端类型 34 债务清理 | P1 | - | done | 16未使用快修+18真实类型全清零; tsc零错误+vitest 47 passed |
| T7 | 质量检测 | P0 | T1-T6 | blocked | 全量巡检 + CI 转绿验证 |

状态值: pending / in-progress / blocked / done

## 关键路径

```
关键路径: T1-T4 (P2 升级, 可并行) + T5-T6 (债务, 可并行) → T7
复用: execution/llm_review.py (#8-10 提取的公共 LLM 审查模块, 消除重复)
范式: 🟡结构预筛 + 🟢LLM 确认 + 降级兜底 (降级时放行不阻断, 见 prompt-upgrade #8-10)
```

## 各任务实施要点

### T1-T4 — prompt-upgrade P2
- 复用 `execution/llm_review.py` 的公共审查接口 (#8-10 已提取, drift/error_loop/tool_loop 共用)
- 每处判断: 先评估"结构校验能否做 🟡 预筛" → 能则混合判断 (预筛+LLM), 不能则纯 LLM
- 降级兜底: LLM 异常/超时时回退到原规则判断或放行, 不阻断主流程
- 同 PR 必须含单元测试 (mock LLM 返回, 验证判断/降级/边界)

### T5 — ruff 债务清理
- `ruff check --fix` 自动修 47 fixable (F401 未使用 import + I001 排序)
- E501 行太长 (69 处) 手动折行, 优先本次新增文件
- 不改逻辑, 纯格式/清理; 修后跑全量 pytest 确认无回归

### T6 — 前端类型债务清理
- 16 未使用变量/import (TS6133/TS6196): 直接删, 零风险
- 18 真实类型 (TS2322/2339/2352/2345): 逐个分析
  - ArtifactEditor/RollbackButton: API client 缺 todos/projects 属性 → 补类型
  - ApprovalDialog/ExperienceList: ReactNode → 修正类型
  - useProjectDetail/SettingsPage: 类型转换 → 明确类型
  - UnifiedWorkspaceView: 多类型 → 收窄联合类型
- 每修一类跑 `npx tsc -b --noEmit` 确认 (提交前必跑, 见 feedback_frontend_tsc_before_commit)

## 对后续版本的约束

- prompt-upgrade 14/14 完成后, 新增 execution 层判断禁止再用规则执行式 (正则/字面匹配/硬编码), 必须走 LLM 或混合判断
- CI 必须保持绿色 (backend-lint + frontend-check), 新增代码不得引入新 ruff/tsc error
- 前端新增组件/改 import 后提交前必跑 tsc -b --noEmit (不能只靠 Vite dev 按需编译)

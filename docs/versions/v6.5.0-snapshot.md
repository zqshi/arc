# v6.5.0 — execution 层拆分评估 + 测试补全 + config 核对（snapshot）

> **主题**: v6.4 收尾后的 3 项遗留技术债务清理 + 健康度修复。无新功能, 纯债务清理
> **状态**: ✅ 已完成（2026-06-25 归档）
> **前置**: v6.4.0 (prompt-upgrade 14/14 全清 + CI 转绿)
> **后续**: v6.6.0 (待定方向)

---

## 版本目标

处理 v6.4 记录的 3 项技术债务: execution 层 500-800 行文件拆分评估 + ui_design_methodology domain 单测补全 + config↔env 字段核对。保持代码库健康度, 无新功能。

## 关键决策

- **T1 execution 拆分评估结论: 保持, 记债务**: 4 文件(tools 601/execution_engine 551/artifact_extractor 546/tool_loop 509)均在 CLAUDE.md 500-800 警告区间(非 >800 必修), 职责内聚。tools.py 拆分需引入兼容间接层(execution_engine.py:374 直接 import 私有函数 _run_command/_write_file, 拆到 tools_impl.py 须重新 export 保持兼容), 减行收益低于复杂度成本。不强拆, 待超 800 或明确拆分动机再拆
- **T2 ui_design 单测补全**: 13 单测覆盖 validate_ui_design 的 wireframe 缺 user_story 检查 + 三态 gap(empty/loading/error, 含 v6.4 T5 补的 error state gap)。domain 层零 mock, 直接构造 content dict 验证行为
- **T3 config↔env 核对(实查差 5 非 3)**: supabase×4 字段(db_url/schema_prefix/anon_key/api_url)补 .env.example BaaS 段; sandbox_builder_images(dict 类型).env.example 已有注释行说明(合理不补 KEY=val)。补全后 config 52 vs env 51, 仅差 dict 类型字段

## 交付物

| ID | 任务 | 状态 |
|----|------|------|
| T1 | execution 层文件拆分评估 | ✅ (结论: 保持记债务) |
| T2 | ui_design_methodology domain 单测 | ✅ (13 单测) |
| T3 | config↔env 差字段核对 | ✅ (补 supabase×4) |
| T4 | 质量检测 | ✅ |

## 质量检测（2026-06-25）

| 项 | 结果 |
|----|------|
| 6.1 死代码 | ✅ T2 新增测试有效引用; T3 .env.example 补充有效配置 |
| 6.2 依赖卫生 | ✅ 无新增外部依赖 |
| 6.3 配置一致 | ✅ T3 补 supabase×4, config↔env 对齐(仅差 dict 类型, 合理) |
| 6.4 文档对齐 | ✅ current 唯一, CLAUDE.md 索引路径全存在 |
| 6.5 架构合规 | ✅ DDD 层级合规; execution 4 文件 500-800 警告(T1 评估保持) |
| 6.6 测试覆盖 | ✅ T2 补 ui_design 13 单测(v6.4 遗留债务清零) |
| 6.7 仓库卫生 | ✅ 无构建产物误入 |

**CI 转绿**: ruff 0 / tsc 0 / unit 1755(+13) / integration 102 / vitest 47, 零回归

## 遗留 / 技术债务

| 项 | 优先级 | 处理 |
|----|--------|------|
| execution 层 4 文件 500-800 行 | P2 | T1 评估保持(职责内聚), 待超 800 或明确拆分动机再拆 |
| config↔env sandbox_builder_images | P3 | dict 类型, .env.example 已注释说明, 合理 |

## 约束

- execution 层 4 文件保持现状(500-800 警告, 评估结论保持), 新增代码注意职责内聚, 超 800 必修
- ui_design 等方法论模块必须有 domain 单测(零 mock)
- config 新增字段必须同步 .env.example

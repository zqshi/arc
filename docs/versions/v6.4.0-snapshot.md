# v6.4.0 — 债务清理 + prompt-upgrade P2（规则残留 LLM 化）（snapshot）

> **主题**: 收尾 Arc "规则执行式→意图驱动" 升级(execution 层 4 处残留规则 LLM 化) + 清零 main 既有 ruff/tsc 债务让 CI 转绿。完成后 prompt-upgrade 14/14 全清
> **状态**: ✅ 已完成（2026-06-25 归档）
> **前置**: v6.3.0 + prompt-upgrade P1 (#8-10)
> **后续**: v6.5.0

---

## 版本目标

两件事收尾 Arc 的"规则执行式→意图驱动"升级与债务清理:
1. prompt-upgrade P2 (#11-14): execution 层 4 处残留规则执行式判断(正则/字面匹配/空参数/硬编码推断) LLM 化, 复用 #8-10 建立的"🟡结构预筛+🟢LLM确认+降级兜底"范式。完成后 14/14 全清
2. 治理债务清理: main 既有 ruff 68 + 前端类型 34, 合并 v5.9-v6.2 时发现, CI 长期红。本版本清零让 CI 转绿

## 关键决策

- **#11 事件时态 LLM 化**: _is_past_tense 字面预筛(🟡) + LLM 判断 DDD 合规(🟢) + 异常回退字面匹配。validate_architecture 改 async, 连锁 _check_methodology/_safe_methodology async
- **#12 测试失败 LLM 化**: 字面预筛 FAIL/ERROR(🟡) + LLM 判断是否真失败(区分测试名/注释字样 vs 实际失败)(🟢) + 降级回退字面
- **#13 route_strategy 空参数修复**: 传入真实 title/description 让关键词路由(NEW_DOMAIN/OPTIMIZATION)生效(空参数退化为纯 round 路由, 丢失语义路由)
- **#14 _infer_phase LLM 化**: 保留 _infer_phase 标准线性流程作🟡预筛 + LLM 推断推进/回退(覆盖非线性返工)(🟢) + 降级回退预筛
- **范式统一**: T1-T4 复用 execution/llm_review.py 的 default_llm_review (llm_review_fn 可注入测试), 🟡预筛+🟢LLM确认+降级兜底, LLM 异常不阻断主流程
- **债务清理零逻辑改动**: T5 E501 折行(代码行隐式拼接/参数换行, prompt 文本源码换行不改语义) + F841 死变量清理; T6 前端 16 未使用快修删 + 18 真实类型修复(ArtifactEditor/RollbackButton 修 api 调用 bug, toast/confirm 类型统一 ToastType/ConfirmOptions)

## 交付物

| ID | 任务 | 状态 |
|----|------|------|
| T1 | #11 architecture_methodology 事件时态 LLM 化 + ruff(F841×2/N806×3/E501×6) | ✅ |
| T2 | #12 dev_test_methodology 测试失败 LLM 化 | ✅ |
| T3 | #13 constraint_policy route_strategy 空参数修复 | ✅ |
| T4 | #14 prompt_builder _infer_phase LLM 化 | ✅ |
| T5 | ruff 68 债务清理(E501×55+F841×2, architecture 11 处留 T1) | ✅ |
| T6 | 前端类型 34 债务清理(16 未使用 + 18 真实) | ✅ |
| T7 | 质量检测 | ✅ |

## 质量检测（2026-06-25）

| 项 | 结果 |
|----|------|
| 6.1 死代码 | ✅ T1-T4 新增函数均在调用链 + 单测覆盖 |
| 6.2 依赖卫生 | ✅ 无新增外部依赖 |
| 6.3 配置一致 | ✅ config↔env env_prefix="ARC_" 映射(差 3 derived 字段, 既有) |
| 6.4 文档对齐 | ✅ current 唯一, CLAUDE.md 索引路径全存在 |
| 6.5 架构合规 | ✅ DDD 层级合规, 无循环依赖; seeds>800 有例外注释, execution 500-800 记债务 |
| 6.6 测试覆盖 | ✅ T1-T4 新增 32 单测; ui_design 无测试记债务 |
| 6.7 仓库卫生 | ✅ 无构建产物误入 |

**CI 转绿**: ruff 0(前68) / tsc 0(前34) / unit 1742 / integration 102 / vitest 47, 零回归

## 遗留 / 技术债务

| 项 | 优先级 | 处理 |
|----|--------|------|
| config↔env 差 3 字段 | P3 | derived/默认字段, 既有非 v6.4 引入, 后续核对 |
| execution 层 500-800 行文件 | P2 | tools(601)/execution_engine(551)/artifact_extractor(546)/tool_loop(509), 下版本评估拆分 |
| ui_design_methodology 无 domain 测试 | P2 | T5 补 error gap 未配测试, 补 test_ui_design_methodology |

## 约束

- prompt-upgrade 14/14 完成后, 新增 execution/context 层判断禁止再用规则执行式(正则/字面匹配/硬编码), 必须走 LLM 或混合判断
- CI 必须保持绿色(backend-lint + frontend-check), 新增代码不得引入新 ruff/tsc error
- 前端新增组件/改 import 后提交前必跑 tsc -b --noEmit

# v6.0.0 Current — 容器化构建 runtime + 原生客户端构建 + sufficiency 接线

> 状态: 进行中
> 起始 commit: 292cc8d (2026-06-24 groundwork)
> 上一版本: [v5.10.0](v5.10.0-snapshot.md)
> 真相源: [docs/prompt-upgrade-plan.md](../prompt-upgrade-plan.md)（#7 属本版本）

## 版本目标

激活 v5.9.0 项目类型框架的**第二个项目类型 `BINARY_APP`**——把原生客户端（桌面/移动）的构建链路打通：容器化构建 runtime 真正可用 + 二进制构建 + 产物落制品目录。同时落地 prompt-upgrade-plan #7（sufficiency 接线）+ ConstraintPolicy 死配置清理。

本版本结束 Arc 只能交付"静态站点"的单类型局限，向"不同项目类型落地"终局迈出第二步。

## 版本验证标准

1. 原生客户端项目经 DockerSandboxRuntime 容器化构建出**容器可构建目标二进制**（linux 二进制 / web / android .apk）
2. `BinaryArtifactDeployer` 把二进制产物落制品目录（不签名不分发，签名在 v6.1）
3. ✅ sufficiency 接线：产出前门禁（产出前判断对话信息是否足够），替代纯轮次计数
4. ConstraintPolicy 死配置清理（gate_threshold 接管后的零引用字段）

> **跨平台范围决策（2026-06-24）**: 容器化 linux 沙箱无法构建 macOS .dmg / Windows .exe（需原生 OS，交叉编译支持有限）。v6.0 聚焦容器可构建目标；mac/win 原生二进制推后（原生 runner 或 CI matrix）。

## 版本范围

**做什么:**
- T1 DockerSandboxRuntime 真实现（✅ groundwork 已完成：真实容器执行 + RW 挂载产物持久化 + 超时/网络/内存限制 + 路径逃逸防护）
- T4 BINARY_APP 框架激活（六处注册点 + ProjectType.BINARY_APP）
- T5 BinaryArtifactDeployer（产物落制品目录，不签名不分发）
- 构建工具链容器镜像（node/rust/tauri/capacitor，聚焦容器可构建目标）
- `run_command` 容器内执行 + 跨平台编译编排
- ✅ #7 sufficiency 接线（已完成，产出前门禁方案）
- T8 ConstraintPolicy 死配置清理

**不做什么:**
- 签名/公证 → v6.1.0（凭证可配置非阻塞）
- 商店分发 + 二进制制品分发层 → v6.2.0
- macOS .dmg / Windows .exe 原生二进制构建 → 推后（原生 runner 或 CI matrix）
- #8-10 执行链路语义化 → v6.1
- #11-14 methodology/prompt_builder 残留 → v6.2
- #13 route_strategy 空参数清理 → v6.2（非 v6.0）

---

## 全路线总览（"不同项目类型落地"终局）

| 版本 | 交付 | 凭证依赖 |
|------|------|---------|
| v5.9.0 ✅ | 项目类型框架 + 部署器多态 + 静态站点型落地 | 无 |
| v5.10.0 ✅ | prompt 升级第一批（门禁+部署+澄清） | 无 |
| **v6.0.0（本版本）** | 容器化 runtime + BINARY_APP 构建链路 + BinaryArtifactDeployer + sufficiency 接线 | 无 |
| v6.1.0 | 签名/公证层（Apple/Windows/Android） | Apple/Win/Play 凭证（未配 skip） |
| v6.2.0 | 商店分发 + 制品分发层 + #11-14 残留规则 | 同上 |

---

## 任务依赖图

```
T1 (DockerSandboxRuntime) ✅groundwork
 └──→ T2 (构建工具链镜像)
        └──→ T3 (run_command 容器执行编排)
               └──→ T4 (BINARY_APP 构建链路)
                      └──→ T5 (BinaryArtifactDeployer)
                             └──→ T6 (验证: 本地构建出二进制)
T7 (#7 sufficiency 接线) ← 独立, 需评审方案
T8 (#13 ConstraintPolicy 清理) ← 独立
T1-T8 ──→ T9 (质量检测)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| T1 | DockerSandboxRuntime 真实现 | P0 | - | done | `application/sandbox/runtime.py`, `tests/unit/application/test_sandbox_runtime.py` |
| T2 | 构建工具链容器镜像 | P0 | T1 | pending | 待定: 复用官方镜像 vs 自建 |
| T3 | run_command 容器内执行 + 跨平台编译编排 | P0 | T2 | pending | `application/sandbox/runtime.py` |
| T4 | BINARY_APP 框架激活(六处注册) | P0 | - | done | `domain/project/value_objects.py`, `domain/deployment/value_objects.py`, `application/deployment/service.py`, `infrastructure/deployer/__init__.py`, `application/context/prompts.py`, `interface/schemas/project.py` |
| T5 | BinaryArtifactDeployer | P0 | T4 | done | `infrastructure/deployer/binary_artifact.py`(新), `tests/unit/application/test_binary_artifact_deployer.py` |
| T6 | 验证: 容器内构建出可构建目标二进制 | P0 | T2,T3,T5 | blocked | 集成测试(聚焦 linux/web/apk, mac/win 推后) |
| T7 | #7 sufficiency 接线 | P0 | - | done | `execution/sufficiency_gate.py`(新), `artifact/service.py`, `context/providers/sufficiency.py` |
| T8 | ConstraintPolicy 死配置清理 | P2 | - | pending | `execution/constraint_policy.py` (注: #13 route_strategy 空参数属 v6.2; 本任务为 gate_threshold 接管后的零引用字段, 范围待验证) |
| T9 | 质量检测 | P0 | T2,T3,T6,T8 | blocked | 全量巡检 |

状态值: pending / in-progress / blocked / done

## 关键路径

```
关键路径: T2 → T3 → T6 → T9 (运行时构建链路)
框架已就位: T1✅ T4✅ T5✅ T7✅ (纯代码, 不依赖运行时)
并行路径: T8(清理) 独立
阻塞: T2 镜像策略决策(复用官方 vs 自建) → 影响 T3-T6
```

## 各任务实施要点

### T1 — DockerSandboxRuntime（已完成）
- groundwork 已实现：真实容器执行 + RW 挂载产物持久化 + 超时/网络/内存限制 + 路径逃逸防护
- 7 项真实 docker 测试通过（`test_sandbox_runtime.py`）

### T4 — BINARY_APP 框架激活（已完成）
- 六处注册点全部激活（v5.9.0 留好的扩展点直接复用，零框架改动）：
  1. `ProjectType.BINARY_APP`（value_objects.py 取消注释）
  2. `DeployType.BINARY_ARTIFACT`（新增，二进制制品部署形态）
  3. `DeployConfig.for_type` 加 BINARY_APP → `cargo tauri build` / `src-tauri/target/release/bundle`
  4. `DeployService._deploy_type_for` 加 BINARY_APP → BINARY_ARTIFACT 映射
  5. `get_deployer` 注册 BINARY_ARTIFACT → BinaryArtifactDeployer
  6. `PROTOTYPE_BUILD_GUIDES` 加 binary_app → BINARY_APP_BUILD_GUIDE（意图驱动格式：目标+约束+上下文）
- schema Literal 扩 `"binary_app"`；前端 ProjectType 类型同步加 `'binary_app'`
- **前端 UI 选择器暂不暴露**：T6 构建链路未就绪，避免"能选但构建不出"。类型层已同步，T6 就绪后放开 UI

### T5 — BinaryArtifactDeployer（已完成）
- 复用 `storage.async_upload_dir`，prefix=`artifacts/{project_id}/{deploy_id}/`
- **与 StaticSiteDeployer 关键差异**: 不要求 index.html（二进制无统一入口），仅校验目录非空；url 指向制品根（不分发，分发在 v6.2 做下载页）
- 测试: 4 单元（成功/缺目录/空目录/不要求 index.html）+ deploy routing 3 新断言
- **限制**: 复用 DEPLOY_MAX_UPLOAD_SIZE(50MB) 默认限制，大二进制产物分发时 v6.1/6.2 放宽

### T7 — sufficiency 接线（已完成）
- **决策**: 产出前门禁方案（用户确认），非 plan 原推荐的轮次预筛+缓存(A+B)
- **理由**: 每轮注入"引导用户多说"不需要 LLM（轮次计数够用）；三维评估的真正价值是"产出前语义质量判断"，是门禁场景而非每轮注入。职责分离：轮次管引导，LLM 管质量判断。引入缓存复杂度高、用户补信息即失效，收益不明
- **实现**:
  - 新增 `execution/sufficiency_gate.py`: `SufficiencyResult` + `evaluate_sufficiency()` 调 `INPUT_SUFFICIENCY_PROMPT` 三维评估(target_users/core_problem/feature_direction)，降级放行（LLM 失败 → sufficient=True，遵循降级原则）
  - 接入 `ArtifactService.confirm()`: requirement_spec 确认前从 todo(title/desc) + 对话历史拼 summary → `evaluate_sufficiency`，sufficient=false 抛 ValueError（route 转 400 带 follow_up_questions）
  - `SufficiencyHintProvider` 简化: 移除 <4 轮质量判断（交门禁），保留 <2 轮早期粗引导
- **覆盖范围**: 对话模式 `confirm_artifact` route。pipeline 模式 `confirm_phase` 走 `evaluate_gate` 内容门禁兜底，sufficiency 漏检记遗留（#7 主语境是对话模式）
- **测试**: 单元 6（gate 逻辑/降级/解析）+ 集成 3（HTTP confirm 阻断/放行/非 requirement_spec 跳过）
- **遗留**: pipeline 模式 sufficiency 漏检（有 evaluate_gate 兜底，非阻塞）；test_health 时序污染仍存（v5.10 遗留）

## 对后续版本的约束

- 激活 BINARY_APP 时复用 v5.9.0 框架三处注册点（ProjectType + get_deployer + PROTOTYPE_BUILD_GUIDES），不改框架
- sufficiency 接线缓存粒度按 conversation 内容指纹，状态变化才重评估
- 凭证配置项在 v6.1.0 引入时必须同步 `.env.example`（配置一致性 6.3）

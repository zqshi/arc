# v6.22.0 Current — v6.21 遗留收尾 + 技术债务清理

> 状态: 已激活 (2026-07-01, v6.21 归档后激活, 维持"有且仅有一个 *-current.md"不变量)
> 起始 commit: (归档激活, 2026-07-01)
> 上一版本: [v6.21.0](v6.21.0-snapshot.md)
> 上下文: v6.21 D1/D3/T1 完成 (LLM 主链路走 DB + 前端拆分), D2/T2 评估后留下版本, 集成测试 4 失败预先存在。本版本收尾 v6.21 遗留 + 技术债务。**方向待用户细化 (可调整范围/增删项)。**

## 下次会话接手清单 (2026-07-01, T5+D2+T6+T7+T8 已完成, T2/T4 pending)

真相源本文件。基线 ruff0 / unit2253 / integ195 passed / tsc-b0 / vitest97 (T5+D2+T6 403不重试+T7扫描接DB凭证+T8 force强制重扫, 零回归)。

| ID | 任务 | P | 依赖 | 状态 | 接手要点 |
|----|------|---|------|------|---------|
| T5 | 集成测试 4 失败修复 | P1 | - | ✅done | 根因: 三文件本地 `cleanup` 为 teardown 模式(yield 在前), v6.16 savepoint 下 commit 退化、DELETE 被外层 rollback 撤销, 从未真正清理 dev DB 预存 openhands 脏数据。修复: 提取到 conftest 统一 `cleanup` fixture 改 setup 模式(DELETE+flush 在 yield 前), savepoint 内 teardown rollback 不破坏 dev DB。被测代码(sync_registry_from_db/CapabilityRepository/API get_db)均复用传入 db_session, flush 可见。附带: test_db_empty_seeds_env/test_seed_idempotent 不再因脏数据巧合通过, 真正测到 env seed 路径 |
| D2 | worker 凭证链路 | P1 | - | ✅done | 选方案(a)复用主凭证+worker_model 覆盖。`acquire_worker(llm_config)` 接受 D1 透传的 llm_config: 有 api_key → `_create_worker_adapter(llm_config)` 复用 provider/api_key/base_url + `settings.worker_model` 覆盖 model(cheap), ephemeral 不缓存(同 acquire_for_project); None/无 api_key → fallback `_WORKER_KEY`(env 兜底)。调用方仅 `orchestration/service.py:278 _run_worker` 传 `self._llm_config`。新增 `tests/unit/application/test_adapter_pool.py` 6 用例覆盖 DB 凭证/env 兜底/不缓存/worker_model 覆盖。注: settings.worker_model 仍全局 env, 多 provider 下可能 model 与凭证不匹配(渐进边界, 同 D1) |
| T6 | 403 重试致 scan 409 (计划外) | P1 | - | ✅done | `resilience._retry`/`chat_stream`/`chat_stream_with_result` 对所有异常重试(含 403), 加 `_is_retryable_error` 4xx(非429)不重试立即抛出。加 2 测试 |
| T7 | 扫描接 DB 凭证 (计划外, B) | P1 | - | ✅done | scanner_analysis 5 处 `acquire()` → `acquire_for_project(llm_config)`; scan_codebase 路由 `resolve_from_project(project,user.id)` → llm_config 透传 start_scan→_run_scan→scan_and_summarize_stream。D1 边界扩展(原"辅助模块不做"), None 时 env 兜底。加 3 unit 测试(scan_task cancel/B透传) |
| T8 | force=true 强制重扫 (计划外, A) | P1 | - | ✅done | scan_manager 加 `cancel(project_id)` (pop task + cancel + await, 不在 lock 内 await 避死锁); scan_codebase force=true 时 cancel 旧 task 再 start_scan, 非 force 仍 409。scan_status 残留由路由补偿置 idle。加 unit 测试覆盖 cancel |
| T2 | Project dict 值对象化 | P2 | - | pending | **评估范围后定**。pipeline_config/conversation_config 嵌套, 现有 entity 方法已封装 merge, 评估 ROI 后定做不做/做哪些 |
| T4 | 质检归档 | P0 | T2 | pending | 收尾, 依赖 T2 完成 |

**推荐开场**: T5/D2/T6/T7/T8 已完成 → T2 (评估后定) → T4。

**遗留 (T7/T8)**: scanning 端点 force+DB凭证端到端未补集成测试 (scan_manager 全局单例+真扫描难 mock), unit 覆盖核心逻辑 (scan_task cancel/B透传); 路由薄层 (route 零逻辑)。记入 6.6 记录项。

## 版本目标

收尾 v6.21 遗留: D2 worker 凭证链路深化 (需先定 worker 语义) + T2 Project dict 值对象化 (评估范围) + 集成测试环境修复 (4 预先失败, DB openhands 残留)。

## 版本验证标准

1. worker 路径走 DB 凭证 (非 env), per-user 隔离, 保留 env 兜底
2. 集成测试 4 失败修复 (DB openhands 残留清理或测试隔离), 全量 integ 绿
3. 质量检测 6.1-6.7 全过, v6.21 链路零回归

## 版本范围

**做什么:**
- D2: per-phase/worker 选用重映射到凭证 id (保留 env 兜底), 需先定 worker 语义 (cheap model 凭证/model 选择)
- T2: Project dict 字段值对象化 (pipeline_config/conversation_config, 评估范围后做)
- T5: 集成测试 4 失败修复 (test_agent_registry_sync + test_capability_api/repository, DB openhands 残留)

**不做什么:**
- OpenHands/Codex 真注入 (blocked runtime, v6.19 遗留)
- 多 worker 凭证缓存 Redis pub/sub (留增强)
- 辅助模块 LLM 调用接 DB (D1 渐进边界, 非验证标准)

---

## 任务依赖图

```
D2 worker 凭证链路 ──→ (需先定 worker 语义, 产品决策)
T2 Project 值对象化 ── 独立 (评估范围)
T5 集成测试修复 ── 独立 (DB 残留/测试隔离)
T4 质检归档 ── 依赖全部
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 (核心) |
|----|------|---|------|------|----------------|
| D2 | per-phase/worker 选用重映射到凭证 id | P1 | - | done | `application/ai/adapter_pool.py` `application/orchestration/service.py` `tests/unit/application/test_adapter_pool.py` |
| T2 | Project dict 字段值对象化 (评估范围) | P2 | - | pending | `domain/project/value_objects.py` |
| T5 | 集成测试 4 失败修复 (DB openhands 残留) | P1 | - | done | `tests/integration/conftest.py` (统一 cleanup fixture) + `test_agent_registry_sync.py` `test_capability_*.py` (删本地 cleanup) |
| T6 | 403 重试致 scan 409 (计划外) | P1 | - | done | `application/ai/resilience.py` (`_is_retryable_error` + 3 处 except) + `tests/unit/application/test_resilience.py` |
| T7 | 扫描接 DB 凭证 (计划外, B) | P1 | - | done | `application/project/scanner_analysis.py` `application/project/scan_task.py` `interface/routes/project/scanning.py` + `tests/unit/application/test_scan_task.py` |
| T8 | force=true 强制重扫 (计划外, A) | P1 | - | done | `application/project/scan_task.py` (cancel) `interface/routes/project/scanning.py` (force 分支) + `tests/unit/application/test_scan_task.py` |
| T4 | 质检 6.x + 归档 | P0 | T2 | pending | - |

## 各任务实施要点

### D2 — worker 凭证链路 (v6.21 遗留)

**现状**: `acquire_worker` → `_WORKER_KEY` → `_create_worker_adapter` (读 `settings.worker_llm_provider`/`worker_model` env)。同 D1 per-user 矛盾 (worker 路径全局 `_WORKER_KEY`, 凭证 per-user)。

**待定 worker 语义** — cheap model 用哪个凭证哪个 model? 候选:
- (a) 用户默认凭证的某 model (`resolve_default_config` + 指定 worker model)
- (b) 单独 worker 凭证指针 (`Project.worker_provider_id` 或 `LLMProvider.worker_model` 字段)
- (c) 保留 env `worker_model` + DB 凭证 (割裂, 语义怪)

需产品决策后选定。倾向 (a) 最简 (复用 D1 用户默认凭证, worker 用其 models 列表中某 cheap model)。

### T2 — Project dict 值对象化 (v6.21 遗留)

评估 `pipeline_config`/`conversation_config` 哪些 dict 值得值对象化。现有 entity 方法 (`update_pipeline_config`/`update_phase_capabilities` 等) 已封装 merge, 评估 ROI 后定范围。

### T5 — 集成测试 4 失败修复 (预先存在, 非 v6.21 引入)

`test_agent_registry_sync::test_db_declarations_override_env` + `test_capability_api::test_create_and_get`/`test_list_with_filter` + `test_capability_repository::test_list_with_filters`。根因 DB 有 openhands/codex 声明残留 (之前测试或 seed 加的未清理)。修复策略: 清 DB 残留 或 测试隔离 (conftest savepoint, v6.16 模式)。

## 关键决策

1. ✅ D2 worker 语义 → 选 (a): 复用主凭证 (D1 resolve_from_project 透传的 llm_config) + `settings.worker_model` 覆盖 model (cheap), `llm_config=None` 时 env 兜底。已实现
2. ✅ T5 集成测试修复策略 → conftest 统一 cleanup 改 setup 模式 (非清 dev DB 单行, 仓库级根治)
3. ✅ T6 403 重试 → 4xx(非 429)不重试立即抛出 (根治 403 占位致 409)
4. ✅ T7 扫描接 DB 凭证 (B) → 扩展 D1 边界: scanner_analysis 走 acquire_for_project(resolve_from_project), 原"辅助模块不做"边界破除, None 时 env 兜底
5. ✅ T8 force 强制重扫 (A) → scan_manager.cancel 取消旧 task, force=true 时调, scan_status 路由补偿
6. T2 值对象化范围 (评估 ROI, 待定)

## 对后续版本的约束

- 延续 v6.21: LLM 全局默认走 DB 用户默认凭证, env 兜底; `resolve_from_project` 统一入口; Agent 主链路走 `acquire_for_project`
- 延续 v6.20 及之前既定约束

## 遗留 (本版本可能留)

- OpenHands/Codex 真注入 (blocked runtime)
- 多 worker Redis pub/sub (增强)
- 辅助模块 LLM 调用接 DB (D1 渐进边界)

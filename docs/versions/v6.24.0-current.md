# v6.24.0 Current — (方向待定)

> 状态: 已激活 (2026-07-01, v6.23 归档后激活, 维持"有且仅有一个 *-current.md"不变量)。**方向待用户细化。**
> 起始 commit: 355ba97 (v6.23 归档 + 架构提案沉淀)
> 上一版本: [v6.23.0](v6.23.0-snapshot.md)
> 上下文: v6.23 已归档 (C1/G1A/D1A/D2/D3/F1, 见 snapshot)。本版本原拟 G1 B/C 作为方向, **经评估不必要单独占版**, 撤回 backlog。等真正方向 (多用户 / 新功能 / R1-R5 环境就绪) 再细化任务表。

## 未完成功能 / 待办候选清单 (2026-07-01 核实)

真相源本文件 + backlog.md。基线 ruff0 / unit 2277 / integ 216 (33s 确定性) / tsc -b 0 / vitest 97。v6.23 已全 push。

### 一、评估后暂不做 (原 v6.24 主干, 撤回)

| ID | 任务 | 评估结论 | 触发条件 (满足任一则启动) |
|----|------|---------|------------------------|
| G1-B | template services LLM 接 DB | **不必要单独占版** — env 凭证可用时无症状 (gate/template 走 env 正常), 仅 env 未启用场景才失败 (同 v6.22 T7 bug 类)。当前 dev env 配智谱, 无 live bug。属"收尾一致性"非修 bug | ① arc 走多用户/计费隔离; ② 用户报告 template/gate 调用失败 (env 未启用); ③ 产品承诺"设置页凭证对所有 LLM 调用生效" |
| G1-C | gate/validator 注入 adapter | 同上 (G1-B/C 同类, gate 在热路径但不增紧迫性, env 坏了才有症状) | 同上 |

**自我教训**: 把 G1 A 的尾巴自动当 v6.24 主干是惯性, 未重校验紧迫性。连续两个收尾版 (v6.23/v6.24) 无意义, 不值得为 G1-B/C 立版。

### 二、blocked 于运行时环境 (需凭证/服务/runner, 非开发独立可解)

| ID | 任务 | 阻断点 | 来源 |
|----|------|--------|------|
| R1 | OpenHands 真注入 | OpenHands 服务未起 + 凭证 | v6.19 遗留 |
| R2 | Codex 真注入 | Codex 凭证/model 未配 | v6.19 遗留 |
| R3 | 三平台构建端到端 | ARC_GHA_TOKEN PAT + 云端 S3 + iOS 需 macOS runner + 鸿蒙需 self-hosted runner | v6.19 M2 |
| R4 | 签名分发端到端 | Windows EV 证书 / Apple Developer 账号 / 华为 .p12 | v6.19 T4/T7/T10 |
| R5 | BaaS provision 真实 Agent 端到端 | 未在运行态用真实 Agent 对话验证到 ARCHITECTURE 产出 | v6.19 遗留 |

### 三、架构升级候选 (暂不落地, 方案已沉淀)

见 [arc-architecture-upgrade-proposal.md](../arc-architecture-upgrade-proposal.md)。核心: 把 charter/gate 治理延伸到工具层 (ToolPlan 式工具治理契约 + 运行起手快照)。触发条件: 接入 3+ 外部工具 / experience 需复现 / 工具误用事故 / 多租户立项。

### 投产前需处理 (配置/操作, 非代码阻断)

- 浏览器前端登录 (localhost:5173/login, demo/demo123) — 解决 task-stream 401
- 生产配 `ARC_JWT_SECRET` (debug 模式才用 insecure default)
- DB LLMProvider: 当前扫描走 env 兜底 (智谱已验证通); 投产若要 per-user 隔离去设置页配并设默认
- worker_model 别名: glm-4.5-air 被智谱服务端重定向到 glm-4.7 (智谱行为, 非 arc bug)

**推荐**: 等方向明确再细化任务依赖表 (无既定 next.md 规划)。候选方向: 多用户/团队协作 / 新功能模块 / R1-R5 视凭证就绪验证 / 架构升级候选视触发条件。

## 版本目标 / 验证标准 / 范围

(待用户细化方向后填充)

---

## 任务依赖图

```
(待方向确定)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| G1-B/C | (暂不做) template/gate LLM 接 DB | P3 | - | deferred (评估后撤回 backlog) | 见上表触发条件 |
| R1-R5 | OpenHands/Codex/构建/签名/BaaS 端到端 | - | 凭证/环境 | blocked | 各 adapter/deployer/baas |
| 架构升级 | ToolPlan 治理 / 运行起手快照 | P3 待触发 | - | deferred (方案已沉淀) | 见 proposal 文档 |

## 对后续版本的约束

- 延续 v6.23: integration AND unit conftest 都须清 adapter_pool; mock fixture 须 patch 两工厂 (create_llm_adapter + create_llm_adapter_from_config)
- 延续 v6.22: worker/扫描路径走 DB 凭证; resilience 4xx (非 429) 不重试
- 延续 v6.21: LLM 全局默认走 DB 用户默认凭证, env 兜底; resolve_from_project 统一入口

## 遗留

- OpenHands/Codex 真注入 (blocked runtime)
- G1 B/C (deferred, 触发条件见上)
- 架构升级候选 (deferred, 见 proposal)

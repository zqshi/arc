# v6.24.0 Current — (方向待定)

> 状态: 已激活 (2026-07-01, v6.23 归档后激活, 维持"有且仅有一个 *-current.md"不变量)。**方向待用户细化。**
> 起始 commit: 355ba97 (v6.23 归档 + 架构提案沉淀)
> 上一版本: [v6.23.0](v6.23.0-snapshot.md)
> 上下文: v6.23 已归档 (C1/G1A/D1A/D2/D3/F1, 见 snapshot)。本版本原拟 G1 B/C 作为方向, **经评估不必要单独占版**, 撤回 backlog。等真正方向 (多用户 / 新功能 / R1-R5 环境就绪) 再细化任务表。

## 未完成功能 / 待办候选清单 (2026-07-01 核实)

真相源本文件 + backlog.md。基线 ruff0 / unit 2277 / integ 216 (33s 确定性) / tsc -b 0 / vitest 97。v6.23 已全 push。

### 一、评估后暂不做 (原 v6.24 主干, 撤回)

| ID | 任务 | 评估结论 | 触发条件 (满足任一则启动) |
|----|------|---------|------------------------|
| G1-B | template services LLM 接 DB | **不必要单独占版** — env 凭证可用时无症状 (gate/template 走 env 正常), 仅 env 未启用场景才失败 (同 v6.22 T7 bug 类)。当前 dev env 配智谱, 无 live bug。属"收尾一致性"非修 bug | ① arc 走多用户/计费隔离; ② 用户报告 template/gate 调用失败 (env 未启用); ③ 产品承诺"设置页凭证对所有 LLM 调用生效" |
| G1-C | gate/validator 注入 adapter | 同上 (G1-B/C 同类, gate 在热路径但不增紧迫性, env 坏了才有症状) | 同上 |

**自我教训**: 把 G1 A 的尾巴自动当 v6.24 主干是惯性, 未重校验紧迫性。连续两个收尾版 (v6.23/v6.24) 无意义, 不值得为 G1-B/C 立版。

### 二、blocked 于运行时环境 (需凭证/服务/runner, 非开发独立可解)

| ID | 任务 | 阻断点 | 来源 |
|----|------|--------|------|
| R1 | OpenHands 真注入 | OpenHands 服务未起 + 凭证 | v6.19 遗留 |
| R2 | Codex 真注入 | Codex 凭证/model 未配 | v6.19 遗留 |
| R3 | 三平台构建端到端 | ARC_GHA_TOKEN PAT + 云端 S3 + iOS 需 macOS runner + 鸿蒙需 self-hosted runner | v6.19 M2 |
| R4 | 签名分发端到端 | Windows EV 证书 / Apple Developer 账号 / 华为 .p12 | v6.19 T4/T7/T10 |
| ~~R5~~ | ~~BaaS provision 真实 Agent 端到端~~ | **v6.24 已端到端验证 (2026-07-01)** — 见下「R5 验证结论」, 推翻"blocked 构建凭证"误判 | v6.19 遗留 |

### 二·一、R5 端到端验证结论 (2026-07-01 v6.24 实测)

**推翻 v6.23/backlog 误判**: R5 原标注"blocked 需 PAT/S3/runner/证书"是错的 — R5 不依赖构建凭证 (那是 R3/R4), 只需真实 LLM + provision 链路。实测在 dev 环境 (智谱 glm-5-turbo 经 anthropic 兼容端点 open.bigmodel.cn) **端到端跑通**, 暴露以下真实问题:

**验证通过项**:
- ✅ LLM 链路: 智谱 glm-5-turbo 真实调用 (非 mock), pipeline ARCHITECTURE 阶段产出 tech_architecture artifact (含 data_model.entities)
- ✅ domain_model 提取: refresh 从 tech_architecture 提取 3 聚合 (Order/Payment/Inventory) 入 project.domain_model
- ✅ BaaS provision 机制本身: 手动触发 try_provision_baas_after_extract 真建出 arc_{project} schema + 业务表 (orders/payments/inventories) + 元模型表 (_meta_*) + baas_instances 记录 active

**严重 bug (投产阻断, 待修)**:
- ❌ **[P0] BaaS provision 在 pipeline 主交付路径不自动触发**: provision 只挂在 conversation 模式的 `artifact_extractor → try_extract_domain_model → try_provision_baas` 链; `pipeline generate` (ArtifactService.generate_from_conversation) 和 `domain-model/refresh` (DomainModelService) **都不接入 artifact_extractor → 不触发 provision**; 且无手动 provision 端点 (baas-status 是只读 GET)。后果: 用户走 pipeline 做 architecture, BaaS 永远不 provision, baas-status 永远 false。修法: pipeline generate 后 / refresh 后接 provision 调用, 或加手动 provision 端点。
- ❌ **[P0] provision 建表启用 RLS 但 0 policy → 非 superuser 角色访问 deny-all**: 实测 orders/payments/inventories `relrowsecurity=t` 但 `pg_policies` 计数 0。PostgreSQL 语义: RLS 启用 + 无 policy = 默认拒绝。Supabase 投产场景前端用 anon/authenticated (非 superuser) → 表不可读写。RLS 校验 warning 已检测 ("缺 authenticated SELECT 策略") 但只告警不修复。dev 同库 arc (superuser) bypass RLS 不受影响, 掩盖了此问题。修法: DomainModelApplier.apply_snapshot 按 user_id 列生成 authenticated SELECT/INSERT policy。

**修复 (2026-07-02, P0-1+P0-2 已端到端验证)**:
- ✅ **P0-1**: `DomainModelService` 加 `provision_baas` 统一入口, `refresh_domain_model` 提取后自动调 + `POST /projects/{id}/baas/provision` 手动端点。实测 pipeline → architecture generate → refresh → baas-status `provisioned=true` (之前永远 false)。
- ✅ **P0-2**: `DomainModelApplier.convert_to_baas_schema` 生成默认 RLS policy (有 user_id→行级隔离 `user_id=auth.uid()` / 无 user_id→authenticated 共享) + user_id 列 `DEFAULT auth.uid()`; `SchemaProvisioner` provision 时预建 Supabase 约定 (authenticated/anon 角色 + auth.uid() 函数, dev 兜底/Supabase 内置跳过)。实测 arc_xxx schema `pg_policies=4` (修复前 0=deny-all)。
- CI 基线: ruff0 / pytest 2500 passed (unit+integ) / 全绿, 无回归。
- 改动文件: `application/baas/domain_model_applier.py` + `infrastructure/baas/{sql_generator,schema_provisioner}.py` + `application/project/domain_model_service.py` + `interface/routes/project/operations.py` (端点) + 3 测试文件。

**次要问题**:
- ⚠️ 智谱 glm-5-turbo architecture 产出 **191s/单次** (completion_tokens=3063, 智谱服务端 x-process-time 190s) — 极慢, 严重影响交互体验。worker_model 重定向 glm-4.5-air→glm-4.7 同源 (智谱行为)
- 🐛 `start_phase` 前置检查放行 active 的 clarification: clarification 产品规则"不可跳过"(skip 返回 400), 但 start architecture 时前置检查 (要求前置 CONFIRMED/SKIPPED) 放行了 active clarification, 绕过"必须完成"约束
- ⚠️ 配置易错点: 根 `.env` 全是空占位符, 真实 LLM 配置在 `backend/.env` (cwd=backend 读); 多 .env + pydantic forbid (ARC_DB_PORT infra 变量) 组合, 启动方式不对即 forbid/空 key 500

**复现路径**: docker db/redis + `cd backend && uvicorn arc.main:app` (读 backend/.env) → demo/demo123 → strict project + todo → pipeline/start → skip ui_design → start architecture → generate (191s LLM) → 手动 refresh → 手动调 try_provision_baas (因 pipeline 不自动触发)。

### 三、架构升级候选 (暂不落地, 方案已沉淀)

见 [arc-architecture-upgrade-proposal.md](../arc-architecture-upgrade-proposal.md)。核心: 把 charter/gate 治理延伸到工具层 (ToolPlan 式工具治理契约 + 运行起手快照)。触发条件: 接入 3+ 外部工具 / experience 需复现 / 工具误用事故 / 多租户立项。

### 投产前需处理 (配置/操作, 非代码阻断)

- 浏览器前端登录 (localhost:5173/login, demo/demo123) — 解决 task-stream 401
- 生产配 `ARC_JWT_SECRET` (debug 模式才用 insecure default)
- DB LLMProvider: 当前扫描走 env 兜底 (智谱已验证通); 投产若要 per-user 隔离去设置页配并设默认
- worker_model 别名: glm-4.5-air 被智谱服务端重定向到 glm-4.7 (智谱行为, 非 arc bug)

**推荐**: 等方向明确再细化任务依赖表 (无既定 next.md 规划)。候选方向: 多用户/团队协作 / 新功能模块 / R1-R5 视凭证就绪验证 / 架构升级候选视触发条件。

## 版本目标 / 验证标准 / 范围

(待用户细化方向后填充)

---

## 任务依赖图

```
(待方向确定)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| G1-B/C | (暂不做) template/gate LLM 接 DB | P3 | - | deferred (评估后撤回 backlog) | 见上表触发条件 |
| R1-R5 | OpenHands/Codex/构建/签名/BaaS 端到端 | - | 凭证/环境 | blocked | 各 adapter/deployer/baas |
| 架构升级 | ToolPlan 治理 / 运行起手快照 | P3 待触发 | - | deferred (方案已沉淀) | 见 proposal 文档 |

## 对后续版本的约束

- 延续 v6.23: integration AND unit conftest 都须清 adapter_pool; mock fixture 须 patch 两工厂 (create_llm_adapter + create_llm_adapter_from_config)
- 延续 v6.22: worker/扫描路径走 DB 凭证; resilience 4xx (非 429) 不重试
- 延续 v6.21: LLM 全局默认走 DB 用户默认凭证, env 兜底; resolve_from_project 统一入口

## 遗留

### v6.24 R5 暴露的次要待办 (2026-07-02, 已入 backlog 技术债务表)

- **[P2] conversation 模式 BaaS 端到端未运行态实测**: provision 触发链只实测 pipeline 路径 (→refresh→provisioned=true), conversation 路径 (send_message→execution_engine→artifact_extractor→try_provision_baas) 仅代码路径确认。新会话续: conversation 发消息产出 tech_architecture → 验 baas-status provisioned。
- **[P2] start_phase 前置检查与 skip 规则不一致**: clarification 产品规则"不可跳过"(skip→400), 但 start architecture 前置检查放行 active 的 clarification, 绕过"必须完成"。修: 前置检查收紧 (active 也算未完成)。
- **[P3] 智谱 architecture 产出慢**: 实测 23s~191s 波动 (智谱服务端 x-process-time, 非 arc bug)。评估 worker_model 切 glm-4.5-air 或换端点。
- **[P3] 多 .env 配置易错**: 根 .env 全空占位 + backend/.env 真实 + pydantic forbid ARC_DB_PORT。统一单 .env 或 config 加 extra='ignore'。
- **[P3] Hooks 未复用 provision_baas**: conversation/pipeline 两处 project→snapshot→apply_snapshot 编排重复。Hooks 改调 DomainModelService.provision_baas。

### 既有遗留

- OpenHands/Codex 真注入 (blocked runtime)
- G1 B/C (deferred, 触发条件见上)
- 架构升级候选 (deferred, 见 proposal)

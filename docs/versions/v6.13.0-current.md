# v6.13.0 Current — 签名链路真实化 + 凭证预留 (A 限定版)

> 状态: 进行中 (方向已选: A 限定版 — Android L3 已验证, 修 P3 + 凭证预留 + L3 固化测试, mac/win 真实验证待凭证)
> 起始 commit: 0e11c0f (L1-L3 闭环)
> 上一版本: [v6.12.0](v6.12.0-snapshot.md)

## 版本目标

v6.12 android 构建链路 L1-L3 闭环后, 推进签名链路真实化 (方向 A 限定版):
Android 签名经 L3 真实验证可用 (apksigner sign/verify 通过); Apple/Windows 真实签名
卡凭证与 Windows runner, 本版做"凭证预留功能配置项" — 修复 v6.1 P3 遗留 (notarytool
--apple-id 应用 Apple ID 邮箱非 team_id), 让凭证配置接口正确, 用户配齐凭证即可真实签名。
mac/win 真实产物签名验证待用户提供凭证 / Windows runner, 不在本版范围。

## ⚠️ 新会话启动清单（必读）

> **本版本状态**: 方向已选 A 限定版 (签名链路真实化 + 凭证预留)。T1 P3 修复 done, T2 L3 固化测试 done, T3 文档收尾 done。
> **上一版本**: v6.12 已归档 (2026-06-28, commit 5ce940e)
> **起始 commit**: 0e11c0f (L1-L3 闭环, 已 push)
> **CI 基线**: ruff0 / pytest unit 1989 (6 slow skip) / integ 136 (3 slow skip) / tsc 0 / vitest 88 / 文件全合规 / DDD 零违规
> **当前分支**: `feat/v6.11-release-readiness` (已 push)

### v6.12 遗留小项 — ✅ 已全部完成 (2026-06-28, commit 0e11c0f)

| # | 待办 | 状态 | 结论 |
|---|------|------|------|
| L1 | runtime `--shm-size` 验证/加固 | ✅ done | `_build_docker_argv` 按 `build_target == CAPACITOR_APK` 加 `--shm-size 2g` (runtime.py)。零依赖单测覆盖 capacitor/tauri/web 三分支 |
| L2 | capacitor kotlin BUILD_GUIDE 提示 | ✅ done | `methodology.py:BINARY_APP_BUILD_GUIDE` 加 capacitor 7 kotlin stdlib 统一提示 (resolutionStrategy.force 1.8.22)。回归断言锁住 checkDuplicateClasses/resolutionStrategy 文案 |
| L3 | android release build 验证 | ✅ done | 真实端到端验证: capacitor 7 → assembleRelease BUILD SUCCESSFUL (5m22s, --shm-size 2g) → release apk → apksigner sign SIGN_OK → verify v1/v2/v3 全 true。证伪"L3 接 v6.1 mock"假设: AndroidSigner 真调 apksigner, 链路真实可用 |

> L1-L3 v6.12 android 链路收尾闭环 (commit 0e11c0f)。
> 待清理技术债 (非本批): runtime.py:107 application→infrastructure.eventbus 惰性 import (v6.7 既有, 函数内绕环, 非本次引入)。

## 版本范围

**做什么:**
- T1: 修 v6.1 P3 遗留 — SigningCredentials 加 apple_id (Apple ID 邮箱), AppleSigner notarytool --apple-id 用 apple_id 非 team_id; 凭证配置接口正确 (用户配凭证即可真实签名)
- T2: 固化 L3 Android 验证为自动化集成测试 (capacitor apk 构建 + apksigner sign/verify, slow, skipif 无镜像)
- T3: current.md 方向落定 + 任务表 + 验证标准

**不做什么:**
- mac 真实签名验证 (需 Apple Developer ID 证书, 用户私有凭证, 待用户提供)
- win 真实签名验证 (需 Windows runner, 本机 macOS 跑不了 signtool)
- 完整 Apple/Windows 真实产物签名 (卡凭证/环境, 不在本版)

## 版本验证标准

- T1: SigningCredentials.apple_id 字段 + has_apple 含之; AppleSigner notarytool --apple-id 用 apple_id (单测断言); 全量 unit/integration 绿
- T2: test_android_build_real.py (slow) 真实构建 apk + apksigner verify v2 通过; test_sandbox_runtime_real_build.py android 工具链 smoke 过
- 配置一致性: schema dict 透传 (apple_id 自动包含), 无 config.py 变更 (凭证项目维度加密存储)
- 文件全合规, DDD 零违规

---

## 任务依赖图

```
T1 (P3 修复 + 凭证预留) ──┐
                         ├── T3 (文档收尾)
T2 (L3 固化测试) ─────────┘
```

T1/T2 互不依赖, 可并行。T3 收尾。

## 任务详情

### T1: P3 修复 + Apple 凭证配置项预留 ✅ done

- domain/deployment/signer.py: SigningCredentials 加 `apple_id: str` (Apple ID 邮箱), has_apple() 含 apple_id
- infrastructure/signer/__init__.py: load_credentials APPLE 分支读 apple_id
- infrastructure/signer/apple.py: notarytool --apple-id 用 apple_id (非 team_id), --team-id 保持 team_id; 注释去 mock 措辞 (命令构造真实, L3 已证 android 同理)
- 测试级联补 apple_id: test_signer/test_apple_signer/test_signer_registry/test_deploy_signing/test_deploy_credentials; 新增 test_apple_missing_id_not_complete (P3 回归) + notarytool --apple-id 断言

### T2: 固化 L3 Android 验证为自动化集成测试 ✅ done

- tests/integration/test_android_build_real.py: TestAndroidBuildEndToEnd.test_release_build_and_sign — capacitor 7 项目 → assembleRelease (L2 kotlin + L1 shm) → apksigner sign/verify (slow, skipif 无镜像)
- tests/integration/test_sandbox_runtime_real_build.py: TestAndroidBuilderImage 工具链 smoke (capacitor/gradle/apksigner/keytool, 秒级, build_target=CAPACITOR_APK 验证 L1 argv 真实容器)
- pyproject.toml: addopts = ["-m", "not slow"] 让 slow 默认 skip (避免 5min 构建污染日常 pytest)

### T3: current.md 方向落定 + 任务表 ✅ done

- 方向落定 A 限定版, 任务表/依赖图/验证标准/范围补齐

## 约束

- 延续 v6.9-v6.12 约束
- 前端 tsc 检查用 `tsc -b --noEmit` 或 `npm run build`
- slow 测试默认 skip (addopts -m "not slow"), 手动 `pytest -m slow` 跑
- 新会话开始: 读本文件 + 执行会话级巡检 (DDD 层级/循环依赖/文件超限)

# v6.3.0 Current — 项目治理规范传递（交付物初始化声明规范）

> 状态: 进行中 (T1+T2 已完成 — project_charter 建模+初始化产出 + CONVENTION_TEMPLATES 注册表按类型特化, 1698 unit+9 integration 全过; T3 可启动)
> 起始 commit: d4ec5b4 (v6.2 + prompt-upgrade #8-10 合并后)
> 上一版本: [v6.2.0](v6.2.0-snapshot.md)
> 真相源: [docs/versions/backlog.md](backlog.md) v6.3.0 段

## 版本目标

把 Arc 自己的迭代治理体系作为"基因"传给交付出的每个项目, 闭环"上下文不断裂"承诺。Arc 帮用户构建项目, 这些项目自身也需要治理规范 (版本迭代/质量门禁/上下文加载)——不传则交付即腐烂。这是"不同项目类型落地"终局第五步: 构建 → 签名 → 分发 → **治理规范传递**。

## 版本验证标准

1. 新建项目时自动产出 project_charter (项目宪章, 按 ProjectType 生成)
2. CONVENTION_TEMPLATES 注册表 (ProjectType → 意图驱动规范模板)
3. 两层传递: 规范文本 + agent 维护规范的自运转能力
4. 不同 ProjectType 生成不同规范 (官网=SEO/PWA/性能, 客户端=签名/分发/跨平台)

## 版本范围

**做什么:**
- project_charter artifact (项目初始化时产出, 等价 CLAUDE.md 但按 ProjectType 生成)
- CONVENTION_TEMPLATES 注册表 (复用 v5.9.0 注册点模式, 与 PROTOTYPE_BUILD_GUIDES/get_deployer/get_distributor 同构)
- 规范模板意图驱动化 (非 Arc 现有规则执行式)
- 两层传递: ① 规范文本 ② agent 维护规范行为 (版本切换/质量检测自运转)

**不做什么:**
- prompt-upgrade-plan 剩余 7 处 → 独立推进 (本版本依赖其完成情况, 可并行)
- v6.0 波次2/3 (web/android 镜像) → 独立推进

---

## 任务依赖图

```
T1 (project_charter artifact 建模 + 初始化产出)
 ├──→ T2 (CONVENTION_TEMPLATES 注册表: ProjectType → 意图驱动规范模板)
 └──→ T3 (两层传递: 规范文本 + agent 维护规范行为)
T1-T3 ──→ T4 (验证: 不同 ProjectType 生成不同规范)
T1-T4 ──→ T5 (质量检测)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| T1 | project_charter artifact 建模 + 初始化产出 | P0 | - | done | `domain/project/charter.py`(新: ProjectCharter 值对象 + ConventionTemplateProvider 接口 + DefaultConventionTemplateProvider 通用意图驱动骨架), entity.py(加 charter 字段+initialize_charter), models/repositories project.py+z15 migration 持久化, workspace_service 注入 provider 产出, context/provider.py 注入 charter 段, schemas/_helpers 输出, test_project_charter unit(18)+integration(7) |
| T2 | CONVENTION_TEMPLATES 注册表 | P0 | T1 | done | `application/project/convention_templates.py`(新: static_site/binary_app 特化意图段落 + CONVENTION_TEMPLATES dict[ProjectType,str] 注册表 + ConventionTemplateRegistry 查表+fallback 通用骨架), workspace_service 默认 provider 改 registry; 复用 v5.9.0 注册点模式(与 get_distributor/get_prototype_guide 同构); test_convention_templates unit(11)+integration 类型差异验证 |
| T3 | 两层传递 (规范文本 + agent 行为) | P0 | T1 | pending | charter 文本 + agent 自运转 (版本切换/质量检测简化交付) |
| T4 | 验证: 不同 ProjectType 生成不同规范 | P0 | T2-T3 | blocked | 集成测试 |
| T5 | 质量检测 | P0 | T1-T4 | blocked | 全量巡检 |
| T6 | 前端类型债务清理 (34 错误, 计划外) | P1 | - | pending | 12 文件; 16 未使用快修 + 18 真实类型; 独立于 T1-T5, 见下方技术债务段 |

状态值: pending / in-progress / blocked / done

## 关键路径

```
关键路径: T1 → T2 → T3 → T4 → T5
复用: v5.9.0 注册点模式 (与 PROTOTYPE_BUILD_GUIDES / get_deployer / get_distributor 同构)
依赖: prompt-upgrade-plan 完成情况 (规范模板必须意图驱动化, 不能复制 Arc 现有规则执行式 CLAUDE.md)
```

## 各任务实施要点

### T1 — project_charter artifact
- 新 artifact: project_charter (项目宪章, 创建项目时初始化)
- 等价 CLAUDE.md 但按 ProjectType 生成 (Arc 自身 CLAUDE.md 是全局, 交付项目 charter 按类型裁剪)
- 复用 artifact 体系 (与 domain_model/convention 同层)

### T2 — CONVENTION_TEMPLATES 注册表
- `CONVENTION_TEMPLATES: dict[ProjectType, str]` — key=ProjectType, value=规范模板
- 复用 v5.9.0 注册点模式 (六处注册点之一, 与 PROTOTYPE_BUILD_GUIDES 同构)
- 规范模板**必须意图驱动格式** (目标+输出契约+上下文), 非 Arc 现有规则执行式 ("文件<500行/必须auth"等硬规则)
- 按类型裁剪: 官网=SEO/PWA/性能规范, 客户端=签名/分发/跨平台规范 (复用 v6.0-v6.2 成果)

### T3 — 两层传递
- ① 规范文本: project_charter 初始化产出 (T1)
- ② agent 维护规范行为: 交付项目内的 agent 能自运转版本切换/质量检测 (只传文本不传机制则文本交付即腐烂)
- 机制传递: 把 Arc 的 docs/versions/ 协议 + current.md 上下文加载 + 任务依赖表 + 质量门禁 简化为可交付版本

## 对后续版本的约束

- 规范模板必须意图驱动化 (不能复制 Arc 现有规则执行式 CLAUDE.md, 待 prompt-upgrade-plan 稳定范式)
- 规范按 ProjectType 裁剪 (官网套重型 DDD 规范是负担, 不是越多越好)
- 复用 v5.9.0 注册点模式 (禁止新造注册机制)
- 依赖 prompt-upgrade-plan (P0/P1/P2 分版本推进) 完成情况决定本版本何时启动

## 计划外技术债务 (合并 v5.9-v6.2 回 main 时发现)

### CI 前端类型检查假绿 (✅ 已修)
- 根因: ci.yml `npx tsc --noEmit` 对根 tsconfig.json (project references 空壳, files:[]) 跑, 不检查任何文件 → 前端类型检查一直空转假绿
- 修复: 改为 `npx tsc -b --noEmit` (与 npm run build 的 tsc -b 检查范围一致, 不输出产物)
- 验证: 修后 exit 2, 查出 34 个既有错误 (证明不再假绿)

### 后端 ruff 既有债务 (main 既有, 非本次引入)
- 164 errors: E501 行太长(69), F401 未使用import(47), I001 import排序(31), 其他(17)
- 影响: CI backend-lint job 红; 含 execution_engine/tool_loop/conversation_service 等 12 处既有错误
- 处理: T6 范围, 可 `ruff check --fix` 自动修 47 fixable, 余下手动; 独立于 v6.3 T1-T5

### 前端既有类型错误 34 个 (main 既有, 非本次合并引入)
- 16 个未使用变量/import 快修 (TS6133/TS6196): SettingsPage(5), UnifiedWorkspaceView(3), ConfirmProvider(3), ValidationPanel(2), useVersionActions(1), Skeleton.test(1), templates(1)
- 18 个真实类型问题 (TS2322/2339/2352/2345): ArtifactEditor/RollbackButton (API client 缺 todos/projects 属性), ApprovalDialog/ExperienceList (ReactNode), useProjectDetail/SettingsPage (类型转换), UnifiedWorkspaceView (多类型)
- 影响: main 前端 CI job 红; 不影响后端 execution 层 (#8-10 prompt-upgrade 工作, 后端单测验证)
- 处理: T6 独立清理, 不阻塞 v6.3 T1-T5; 16 快修零风险可先清, 18 真实问题需逐个分析

# v6.17.0 Next — 方向待定

> 状态: 方向待定 — v6.16 技术债务清理归档后, 下一版本产品方向待定。激活时改名为 `v6.17.0-current.md`。
> 上一版本: [v6.16.0](v6.16.0-snapshot.md)
> 上下文: v6.16 完成 v6.15 遗留 4 项 P2 技术债务清理 (execution_mode 下线 / gate 阈值同源 / 死结构清理 / 测试隔离)。下一版本可承接 backlog pending 技术债务, 或定产品方向。

## 候选

- backlog pending 技术债务 (application 层 service 测试 / 聚合边界重构 / 前端测试体系等, 见 backlog.md 技术债务表)
- 产品方向待定

## 对后续版本的约束

- 延续 v6.16: process_constraint 单一真相源 (禁止再引入 execution_mode 或 todo.process_constraint 字段); gate 阈值读 GateProfile (禁止硬编码 score); 测试 DB 隔离用 savepoint (禁止 setup commit + teardown rollback)。
- 延续 v6.15: 依赖约束 (DAG) 三档共享硬不变量; ProcessConfig 仅持 constraint; pipeline 写操作受模式守卫保护。

# v6.25.0 Current — (方向待定)

> 状态: 已激活 (2026-07-02, v6.24 归档后激活, 维持"有且仅有一个 *-current.md"不变量)。**方向待用户细化。**
> 起始 commit: 2baedd0 (v6.24 归档)
> 上一版本: [v6.24.0](v6.24.0-snapshot.md)
> 上下文: v6.24 已归档 (BaaS 端到端 + strict gate 死循环修复, 见 snapshot)。strict gate 原标 "v6.25 专修" 提前在 v6.24 完成, v6.25 方向重新待定。

## 候选方向 (2026-07-02 核实)

真相源本文件 + backlog.md。基线 ruff0 / unit 2300 / integ 223 (共 2523) / tsc -b 0 / vitest 97。v6.24 已全 push。

### 一、v6.24 遗留可独立解 (开发侧)

| ID | 任务 | P | 来源 |
|----|------|---|------|
| L1 | DomainModelExtractor 清洗 entity name (治本, 评估消费者 aggregate_relations/UI) | P1 | v6.24 conversation 实测 |
| L2 | start_phase 前置检查收紧 (active clarification 也算未完成) | P2 | v6.24 R5 验证 |
| L3 | strict gate 逃生阀频率限制 (防滥用退化为伪 STRICT) | P3 | v6.24 P2 遗留 |
| L4 | pipeline/service.py 抽 `_handle_review_failure` (483→<500) | P3 | v6.24 P2 遗留 |
| L5 | rubric 化 score 分布量化对照 (冻结产出物夹具) | P3 | v6.24 P1 遗留 |
| L6 | 智谱 architecture 慢 (切 glm-4.5-air / 换端点) | P3 | v6.24 R5 验证 |
| L7 | 多 .env 整理 (config `extra='ignore'`) | P3 | v6.24 R5 验证 |

### 二、blocked 于运行时环境 (需凭证/服务/runner)

| ID | 任务 | 阻断点 | 来源 |
|----|------|--------|------|
| R1 | OpenHands 真注入 | 服务未起 + 凭证 | v6.19 遗留 |
| R2 | Codex 真注入 | 凭证/model 未配 | v6.19 遗留 |
| R3 | 三平台构建端到端 | PAT + 云端 S3 + iOS macOS runner + 鸿蒙 self-hosted | v6.19 M2 |
| R4 | 签名分发端到端 | Windows EV 证书 / Apple Developer / 华为 .p12 | v6.19 T4/T7/T10 |

### 三、deferred (触发条件见 backlog)

- G1 B/C (template/gate LLM 接 DB): 触发条件 ① 多用户/计费 ② 用户报失败 ③ 产品承诺设置页凭证全生效
- 架构升级候选 (ToolPlan 工具治理): 触发条件 3+ 外部工具 / experience 复现 / 工具误用 / 多租户立项

**推荐**: 等方向明确再细化任务依赖表。候选: ① L1-L7 收尾债务 (L1/L2 优先, 有真实 bug/体验); ② 多用户/团队协作新模块; ③ R1-R4 视凭证就绪; ④ 架构升级视触发。

## 版本目标 / 验证标准 / 范围

(待用户细化方向后填充)

---

## 任务依赖图

```
(待方向确定)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| L1-L7 | v6.24 遗留收尾债务 | P1-P3 | - | pending (候选) | 见上表 |
| R1-R4 | OpenHands/Codex/构建/签名 端到端 | - | 凭证/环境 | blocked | 各 adapter/deployer |
| G1 B/C + 架构升级 | template/gate 接 DB / ToolPlan | P3 待触发 | - | deferred | 见 backlog |

## 对后续版本的约束

- **延续 v6.24 (gate 链路)**: `evaluate_gate` / `evaluate_conversation_gate` 都支持 `llm_review_fn` 注入测试 (签名 `(prompt)->dict`); `GateResult.review_infra_failure` 哨兵标识 LLM 评审故障; `GATE_EVALUATION_PROMPT` 输出契约为 `{score, p0_gaps, gaps, suggestion}` (**无 passed**, 由代码按 score 推导); confirm 故障逃生阀仅 `review_infra_failure=True` 可 force
- 延续 v6.23: integration AND unit conftest 都须清 adapter_pool; mock fixture 须 patch 两工厂
- 延续 v6.22: worker/扫描路径走 DB 凭证; resilience 4xx (非 429) 不重试
- 延续 v6.21: LLM 全局默认走 DB 用户默认凭证, env 兜底; `resolve_from_project` 统一入口

## 遗留

见 [v6.24.0-snapshot.md](v6.24.0-snapshot.md) 「遗留问题」(L1-L7 技术债 + R1-R4 blocked runtime + G1 B/C + 架构升级 deferred)。

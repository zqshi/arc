# v6.14.0 Snapshot — 项目设置 UX 整理

> 归档于 2026-06-28 (commit 0d86c96 + 53069df 之前的前端 T1)
> 上一版本: [v6.13.0](v6.13.0-snapshot.md)
> 下一版本: [v6.15.0](v6.15.0-current.md)

## 目标

环节能力配置 (v6.8.0 已建成) 作为平级独立卡片排在项目规范上方且无收起机制, 对大多数用默认配置的项目是视觉噪声。重组为"高级配置项"——移到「项目规范」卡片下方, 默认收起, 点击展开。纯前端, 后端零改动。

## 交付

- **T1 (done)**: PhaseCapabilitiesSection 自带折叠 (复用 LLMConfigSection 模式: showAdvanced state 默认 false + ChevronDown rotate-180 + 条件渲染) + SettingsTab 把它挪到「项目规范」卡片下方 + 测试适配 (5 现有测试加 expand() 先展开再断言 + 新增 collapses-by-default-expands-on-toggle)

## 决策

- 复用 LLMConfigSection.tsx:119-130 的折叠约定 (showAdvanced + ChevronDown rotate-180), 不新建通用 Collapsible 组件。出现第 3 处折叠需求再抽。
- 折叠状态纯组件 state, 不持久化 (localStorage), 同 LLMConfigSection。
- 默认行为不变: 7 环节始终存在, 每环节默认 0 能力, 用户可逐环节勾选即时保存 (PUT phase-capabilities)。

## 遗留

无 (单任务纯前端, 后端零改动)。

## 约束

- 环节能力配置位于「项目规范」下方, 默认收起; 改其他 settings 区块布局时注意此相对位置。
- 折叠沿用 LLMConfigSection 约定。
- 延续 v6.9-v6.13 约束; 前端 tsc 用 tsc -b --noEmit。

## 质量检测 (归档时执行)

- 6.1 死代码: PhaseCapabilitiesSection/SettingsTab 均在 import 链上 ✓
- 6.2 依赖卫生: 无新前端依赖 ✓
- 6.3 配置一致性: 后端零改动, 无 config 变更 ✓
- 6.4 文档对齐: 索引路径有效, 仅一个 *-current.md ✓
- 6.5 架构合规: 纯前端组件改动, 无 DDD 层级影响 ✓
- 6.6 测试覆盖: 6 现有+1 新增折叠测试 ✓
- 6.7 仓库卫生: 无构建产物误入 ✓
- CI 基线: tsc -b 0 / vitest 89 / build 0

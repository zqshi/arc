# v5.8.0 — 技术债务清理（snapshot）

> **主题**: 文件超限拆分 + 模板 embed 自动化
> **状态**: ✅ 已完成（2026-06-24 归档）
> **前置**: v5.7.0

---

## 版本目标

1. tool_loop.py (511行) 拆分 → metrics + adapters + loop
2. pipeline/service.py (503行) 拆分 → 主类 + hooks
3. prompts.py (504行) 拆分 → artifact_schemas + prompts
4. 模板 release hook 自动生成 embedding

---

## 关键决策

### 拆分原则：行为不变 + import 兼容
- 纯重构，测试全过即成功
- 拆出模块后主文件 re-export 保持调用方 import 路径不变

### tool_loop 拆分
- `tool_loop_metrics.py`: ToolLoopMetrics/Event + 常量（纯数据）
- `tool_loop_adapters.py`: build_openai_messages/parse_*/extract_usage_tokens（无状态函数）
- `tool_loop.py`: ToolAwareLoop 主类 + re-export（413行）

### pipeline/service 拆分
- `pipeline/hooks.py`: 6 个阶段确认 hook（experience/github/confidence/prior_artifacts/domain_model/deployment）提为模块函数
- service.py 调用点改用 pipeline_hooks.*（380行）

### prompts 拆分
- `artifact_schemas.py`: ARTIFACT_SCHEMAS dict 独立（250行，纯数据）
- prompts.py re-export（274行）

### 模板 embed 自动化
- extract_template 末尾调 _generate_embedding（标题+描述+模式作 embed 文本）
- 失败返回 None（不阻断，匹配时该模板不参与向量搜索）
- 修复 v5.7.0 遗留：匹配不再依赖手动设 embedding

---

## 交付物

| ID | 任务 | 估时 |
|----|------|------|
| T1 | tool_loop 拆分 | 40min |
| T2 | pipeline/service 拆分 | 35min |
| T3 | prompts 拆分 | 25min |
| T4 | 模板 embed 自动化 | 25min |
| T5 | 质量检测 | 15min |

---

## 质量检测（2026-06-24）

| 项 | 结果 |
|----|------|
| 6.1 死代码 | ✅ 拆出模块均被 import |
| 6.2 依赖卫生 | ✅ 未改依赖文件 |
| 6.3 配置一致性 | ✅ 未改 config |
| 6.4 文档对齐 | ✅ |
| 6.5 架构合规 | ✅ 全源文件回归 500 行内（仅剩 seeds/api.ts/types 例外）|
| 6.7 仓库卫生 | ✅ git clean |

---

## 遗留 / 技术债务

| 项 | 优先级 | 处理 |
|----|--------|------|
| `execution/tools.py` 567 行 | P3 | 已加例外注释（tool 注册中心）|
| seeds/*.py 超 500 | P3 | 纯数据文件，CLAUDE.md 例外 |
| frontend/api.ts 746 行 | P3 | 纯类型文件，CLAUDE.md 例外 |
| MCP stdio transport | P3 | v5.6.0 遗留 |
| docker-compose Supabase | P2 | 生产部署时补 |
| Higress 网关 | P3 | 部署架构选项 |

---

## 约束

- 拆分是纯重构，行为不变，测试全过
- 后续新增 tool/schema 持续在对应子模块维护，避免主文件再膨胀

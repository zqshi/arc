# v6.18.0 Current — 投产治理升级

> 状态: 已激活 — v6.18 方向已定为投产治理升级。2026-06-29 会话: ① backlog 5 条虚假🔴(v6.16/v6.9 已修但归档漏回写)经实测回写✅释放虚假阻塞 ② CI 补 builder 镜像发布 job (方案A: ghcr matrix job + config 覆盖说明, BINARY_APP 投产阻塞消除) ③ v6.17 snapshot 质检小节核实已存在 ④ 投产前质量检测发现 lint / import 环 / 配置对齐 / 仓库卫生 / 镜像验证阻断项, 本版本目标转为全方位治理升级。
> 上一版本: [v6.17.0](v6.17.0-snapshot.md)
> 上下文: v6.17 完成 skill 对称注入执行链 + 工具集补全 (inline function + MCP 引用)。v6.18 不新增产品功能, 只处理投产上线前阻断项与验证闭环。

## 目标

- 消除投产阻断项: 后端 lint、Python import 环、配置样例对齐、仓库卫生、Docker 镜像验证。
- 固化投产验证命令, 形成可重复执行的 release gate。
- 不扩大产品功能范围, 不改动 DDD 领域行为。
- ~~平台能力运行时缺口~~ ✅ v6.18 解决 (2026-06-29): `docker-publish.yml` 新增 `build-builder-images` matrix job (tauri/web/android-builder → ghcr, release tag 触发); config + .env.example 补生产覆盖映射 (开发态 Makefile 零改, 生产走 `ARC_SANDBOX_BUILDER_IMAGES`)。BINARY_APP 原生客户端构建从"生产需手动 make"推到"CI 自动发布 + config 覆盖拉取"

## 任务依赖表

| ID | 任务 | 状态 | 依赖 | 说明 |
|----|------|------|------|------|
| T1 | 方向定夺: v6.18 定位为投产治理升级 | done | - | 用户已明确以投产为目标进行全方位治理升级 |
| T2 | 后端 lint 清零 | done | T1 | `ruff check .` 已通过; Alembic 历史迁移作为生成文件集中豁免长行 |
| T3 | Python import 环治理 | done | T1 | LLM/EventBus/Sandbox/Context 拆出契约/工厂/工具模块, import 环 5→0 |
| T4 | 配置样例与 Settings 双向对齐 | done | T1 | `.env.example` + `backend/.env.example` 补有效 `ARC_SANDBOX_BUILDER_IMAGES=` |
| T5 | 仓库卫生治理 | done | T1 | 清理缓存/构建产物; `.env`/`.venv`/`node_modules` 保留为本地环境 |
| T6 | 投产验证闭环 | done | T2,T3,T4,T5 | 单测/集成/前端测试/构建/compose/docker 镜像验证全部通过 |
| T7 | 文档回写与上线结论 | done | T6 | 本节已记录验证结果; 当前代码质量满足投产上线 gate |

## 投产验证结果

执行日期: 2026-06-29

| 检测项 | 结果 | 备注 |
|--------|------|------|
| 后端 lint | ✅ 通过 | `cd backend && ruff check .` |
| DDD 分层 import | ✅ 通过 | domain/application/infrastructure 方向扫描 0 违规 |
| Python import 环 | ✅ 通过 | 自研 AST 扫描 `IMPORT_CYCLES 0` |
| Settings ↔ env example | ✅ 通过 | root/backend 两份 `.env.example` 均 0 缺失 |
| 后端单元测试 | ✅ 通过 | `2051 passed, 6 deselected`; 保留 1 个既有 AsyncMock warning |
| 后端集成测试 | ✅ 通过 | `149 passed, 3 deselected` |
| 前端测试 | ✅ 通过 | `89 passed`; 保留 React 测试期 stderr warning |
| 前端生产构建 | ✅ 通过 | PWA 产物生成; 保留既有 >500k chunk warning |
| Docker Compose config | ✅ 通过 | dev/prod 配置渲染通过 |
| 后端 app import | ✅ 通过 | `APP_IMPORT_OK Arc` |
| Docker 镜像构建 | ✅ 通过 | `arc-backend:quality-check` 与 `arc-frontend:quality-check` 均构建成功 |
| 文件行数巡检 | ✅ 通过 | 本次新增/修改结构文件未超限; 5 个既有 seed/type 例外文件保留 |

## 待办（历史接手记录）

- **v6.17.0-snapshot.md 质量检测小节** — ✅ 已核实 (2026-06-29): snapshot 第 44-52 行已有完整 `## 质量检测` 小节 (6.1-6.7 全覆盖 + CI 基线 ruff0/pytest unit2044+integ149/tsc-b0/vitest7), 为 v6.17 归档时初版, 无需补抄。上一会话末尾"未落 snapshot"判断有偏差。

- ✅ **CI 补 builder 镜像发布 job (已实现方案A)** — `docker-publish.yml` 新增 `build-builder-images` matrix job (tauri/web/android-builder, 推 `ghcr.io/<repo>-xxx-builder:latest`+semver+sha, release tag 触发); config.py + .env.example 补 ghcr 生产覆盖映射示例 (开发态 Makefile 零改, 生产走 `ARC_SANDBOX_BUILDER_IMAGES` 覆盖, policy_resolver 覆盖机制已存在无需改)。验证: YAML valid + ruff0 + policy_resolver 覆盖单测19passed + web-builder 本地build成功。待真实 release tag 触发 GHA 验证三镜像发布。

- **本次改动待提交** — 两类工作分两 commit: (a) 归档激活+对齐: git mv next→current + current.md + backlog.md 6条虚假债务回写 (b) CI 功能: docker-publish.yml builder job + config.py/.env.example 覆盖说明。

> 教训记录 (本次): 归档质检 grep/find 三连假阳性 (Dockerfile 命名变体 / 死代码初筛 / config 注释行), 已存 memory `feedback-archive-quality-grep-variants`, 新会话质检时勿重蹈。

## 对后续版本的约束

- 延续 v6.17: skill 取数统一走 CapabilityProvider.load_phase_skills (禁止 TaskContextBuilder 重复实现); skill 工具集 ToolSpec 唯一表达 (source=inline|mcp); CapabilityType.MCP 已实现 (非预留)
- 延续 v6.16: process_constraint 单一真相源; gate 阈值读 GateProfile; 测试 savepoint 隔离
- 延续 v6.15: DAG 三档共享硬不变量; ProcessConfig 仅持 constraint

# v6.18.0 Current — 方向待定

> 状态: 已激活 — v6.17 归档后激活, 恢复"有且仅有一个 *-current.md"协议不变量。2026-06-29 会话: ① backlog 5 条虚假🔴(v6.16/v6.9 已修但归档漏回写)经实测回写✅释放虚假阻塞 ② CI 补 builder 镜像发布 job (方案A: ghcr matrix job + config 覆盖说明, BINARY_APP 投产阻塞消除) ③ v6.17 snapshot 质检小节核实已存在。产品方向仍待定, 候选见下。
> 上一版本: [v6.17.0](v6.17.0-snapshot.md)
> 上下文: v6.17 完成 skill 对称注入执行链 + 工具集补全 (inline function + MCP 引用)。下一版本可承接 backlog pending 技术债务, 或定产品方向。

## 候选

- backlog pending 技术债务 (application 层 service 测试 / 聚合边界重构 / 前端测试体系等, 见 backlog.md 技术债务表)
- v6.17 遗留: MCP 真注入扩展到 OpenHands (runtime config) / Codex (function call 路由); McpClient 完整 SSE 流; ClaudeCode mcp_config 临时文件生命周期清理
- 产品方向待定
- ~~平台能力运行时缺口~~ ✅ v6.18 解决 (2026-06-29): `docker-publish.yml` 新增 `build-builder-images` matrix job (tauri/web/android-builder → ghcr, release tag 触发); config + .env.example 补生产覆盖映射 (开发态 Makefile 零改, 生产走 `ARC_SANDBOX_BUILDER_IMAGES`)。BINARY_APP 原生客户端构建从"生产需手动 make"推到"CI 自动发布 + config 覆盖拉取"

## 待办（新会话接手 — 2026-06-29 会话末尾沉淀）

> 本次会话已完成: ① 激活 v6.18.0-current 恢复版本协议不变量 ② v6.17 归档质检 6.1-6.7 必修项补检(全过) ③ 深挖 builder 镜像真相(纠正假阳性, 确认架构完整非空头支票)。以下未完善项留待新会话, 不在本次继续推进。

1. **v6.18 方向定夺（阻塞项, 需用户决策）** — 见候选区四选项。定方向后据此建任务依赖表, current.md 从"方向待定"转正常版本。

2. **v6.17.0-snapshot.md 质量检测小节** — ✅ 已核实 (2026-06-29): snapshot 第 44-52 行已有完整 `## 质量检测` 小节 (6.1-6.7 全覆盖 + CI 基线 ruff0/pytest unit2044+integ149/tsc-b0/vitest7), 为 v6.17 归档时初版, 无需补抄。上一会话末尾"未落 snapshot"判断有偏差。

3. ✅ **CI 补 builder 镜像发布 job (已实现方案A)** — `docker-publish.yml` 新增 `build-builder-images` matrix job (tauri/web/android-builder, 推 `ghcr.io/<repo>-xxx-builder:latest`+semver+sha, release tag 触发); config.py + .env.example 补 ghcr 生产覆盖映射示例 (开发态 Makefile 零改, 生产走 `ARC_SANDBOX_BUILDER_IMAGES` 覆盖, policy_resolver 覆盖机制已存在无需改)。验证: YAML valid + ruff0 + policy_resolver 覆盖单测19passed + web-builder 本地build成功。待真实 release tag 触发 GHA 验证三镜像发布。

4. **本次改动待提交** — 两类工作分两 commit: (a) 归档激活+对齐: git mv next→current + current.md + backlog.md 6条虚假债务回写 (b) CI 功能: docker-publish.yml builder job + config.py/.env.example 覆盖说明。

> 教训记录 (本次): 归档质检 grep/find 三连假阳性 (Dockerfile 命名变体 / 死代码初筛 / config 注释行), 已存 memory `feedback-archive-quality-grep-variants`, 新会话质检时勿重蹈。

## 对后续版本的约束

- 延续 v6.17: skill 取数统一走 CapabilityProvider.load_phase_skills (禁止 TaskContextBuilder 重复实现); skill 工具集 ToolSpec 唯一表达 (source=inline|mcp); CapabilityType.MCP 已实现 (非预留)
- 延续 v6.16: process_constraint 单一真相源; gate 阈值读 GateProfile; 测试 savepoint 隔离
- 延续 v6.15: DAG 三档共享硬不变量; ProcessConfig 仅持 constraint

# v6.23.0 Current — (方向待定)

> 状态: 已激活 (2026-07-01, v6.22 归档后激活, 维持"有且仅有一个 *-current.md"不变量)
> 起始 commit: (归档激活, 2026-07-01)
> 上一版本: [v6.22.0](v6.22.0-snapshot.md)
> 上下文: v6.22 已归档 (v6.21 遗留收尾 + 扫描链路修复, D2/T5/T6/T7/T8 done + T2 评估不做)。本版本方向待用户细化。

## 下次会话接手清单 (2026-07-01, 投产检测后核实 — 未完成功能待办)

真相源本文件。基线 ruff0 / unit 2277 / integ 216 passed (33s 确定性, F1 修复后) / tsc -b 0 / vitest 97。v6.22 已 push (c5b7ec0)。本会话 4 commit 未 push: D2 (0433fb7) + D3 (635ff72) + docs (163fdc9) + F1 (1f7f559)。后端 8000 (PID 11450) 跑着智谱 GLM (glm-5-turbo/glm-4.5-air), 投产检测全链路通 (扫描 completed + summary 持久化)。

**未完成功能清单 (基于代码核实, 非 backlog 推断):**

### 一、代码层面真未实现 (stub) — ✅ C1 已完成 (2026-07-01 本会话, 参照 claude_code CLI 子进程模式 + `agent` 命令名修正)

| ID | 任务 | P | 说明 | 位置 |
|----|------|---|------|------|
| C1 | Cursor agent adapter 补实现 | P2 | ✅ done: 4 方法实现 + implemented=True + 6 集成测试全绿; Cursor CLI(`agent -p`)同构 claude_code, 无 JSON/stdin/`--mcp-config`(文本降级) | `application/agent/adapters/cursor.py` `tests/integration/test_agent_cursor.py` |

### 二、代码已实现, blocked 于运行时环境 (需凭证/服务/runner — 非开发独立可解)

| ID | 任务 | 代码状态 | 阻断点 | 来源 |
|----|------|---------|--------|------|
| R1 | OpenHands 真注入 | ✅ 真连 (openhands_client.create_session/send_task) | OpenHands 服务未起 (localhost:3000) + 凭证 | v6.19 遗留 |
| R2 | Codex 真注入 | ✅ 真连 (httpx.post 调 Codex API) | Codex 凭证/model 未配 | v6.19 遗留 |
| R3 | 三平台构建端到端 | ✅ 代码链路全通 (v6.19 续6/续7) | ARC_GHA_TOKEN PAT + 云端 S3 + iOS 需 macOS runner + 鸿蒙需 self-hosted runner | v6.19 M2 |
| R4 | 签名分发端到端 | ✅ 代码链路通 (graceful skip 兜底) | Windows EV 证书 / Apple Developer 账号 / 华为 .p12 | v6.19 T4/T7/T10 |
| R5 | BaaS provision 真实 Agent 端到端 | ✅ 代码实现 (schema_provisioner.provision) | 未在运行态用真实 Agent 对话验证到 ARCHITECTURE 产出 | v6.19 遗留 |

### 三、渐进边界 (部分完成, 非阻断)

| ID | 任务 | 现状 | 缺口 |
|----|------|------|------|
| G1 | 辅助模块 LLM 接 DB 凭证 | A 类 9 处已接 DB (2026-07-01 本会话: experience×4/planning×3/project/artifact + helper `resolve_for_context` + conftest 缓存隔离 fixture) | B 类(template matching/apply/extraction/pipeline gate) + C 类(execution detector `default_llm_review` 等 + analysis_service L160 已部分接) 留 v6.24, 需架构决策(注入 vs 签名) |

### 四、增强 + 技术债务 (非阻断, 记录)

| ID | 任务 | P | 说明 |
|----|------|----|------|
| E1 | 多 worker 凭证缓存 Redis pub/sub | P3 | 多 uvicorn worker 下凭证变更跨进程同步, 当前单 worker 不触发 |
| D1 | infra 变量前缀设计债务 | P2 | ✅ A 类 done (2026-07-01): 核实 forbid 已不触发 (config.py 无 model_config, 默认 ignore; v6.18 已注释化 backend/.env.example 的 ARC_WORKERS) → D1 退化为清晰度问题; .env.example 文件头修正前缀语义 + 5 compose infra 变量标注 [compose infra]; B 类(重命名 COMPOSE_) breaking 大 (docker-compose+k8s+Dockerfile+2 .env) 收益低 (forbid 已不触发) 留专项 |
| D2 | conversation_config 子结构值对象化 | P3 | ✅ done (2026-07-01): GitSyncConfig VO + LoopConfig 迁 domain (+from_conversation_config/from_dict, 默认值经 VO 实例单一源, drift guard 测试对齐 DEFAULT_CONVERSATION_CONFIG); Project 加 git_sync_config()/loop_config() 访问器; session_manager + execution_helpers 两处裸读改走访问器; 20 新测试。核实实际裸读 2 站点 (非"7+处") |
| D3 | scanning 端点端到端集成测试 | P3 | ✅ done (2026-07-01): 15 测试覆盖 status/start/stream 三端点。核实推翻"需重构"判断 — scan_manager 虽全局单例, 但路由函数内 lazy import 每次重解析模块属性 → monkeypatch 注入 testdouble 即可, 无需重构。monkeypatch compute_scan_fingerprint + resolve_from_project 保确定性 (0.95s) |
| F1 | test_pipeline_e2e mock_llm 失效修复 | P2 | ✅ done (2026-07-01, 1f7f559): 双层根因 — (1) mock_llm 仅 patch create_llm_adapter 漏 generate 路径 (走 adapter_pool.acquire_for_project→create_llm_adapter_from_config, 未 mock→真实 GLM); 修: 同时 patch 两工厂。(2) integration/conftest 漏 adapter_pool 清理 (G1A 仅给 unit 加), 前测试缓存真实 adapter 污染后测试; 修: 加 _clear_adapter_pool_cache autouse (mirror unit)。效果: 集成套件 15min+flaky → 33s+216 passed 确定性全绿, test_pipeline_e2e/test_agent_registry_sync 多源 flake 一并消除 |

### 投产前需处理 (配置/操作, 非代码阻断)

- 浏览器前端登录 (localhost:5173/login, demo/demo123) — 解决 task-stream 401 (前端 localStorage 无 access_token)
- 生产配 `ARC_JWT_SECRET` (debug 模式才用 insecure default, 非debug+空 → raise)
- DB LLMProvider: 当前扫描走 env 兜底 (智谱已验证通); 投产若要 per-user 隔离去设置页配并设默认
- worker_model 别名: glm-4.5-air 被智谱服务端重定向到 glm-4.7 (智谱行为, 非 arc bug)

**推荐开场**: C1 Cursor adapter (纯开发无依赖, 唯一真代码缺口) → G1 辅助模块 LLM 接 DB (渐进边界收口) → R1-R5 视凭证/环境就绪验证 → 技术债务按需。

**注**: R1-R5 非开发能独立解, 需外部资源 (服务/凭证/runner/证书); 等用户明确 v6.23 方向后细化任务依赖表。无既定 next.md 规划。

## 版本目标

(待用户细化)

## 版本验证标准

(待用户细化)

## 版本范围

**做什么:**
- (待定)

**不做什么:**
- (待定)

---

## 任务依赖图

```
(待定)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 (核心) |
|----|------|---|------|------|----------------|
| C1 | Cursor agent adapter 补实现 | P2 | - | done | `application/agent/adapters/cursor.py` + `tests/integration/test_agent_cursor.py` |
| G1 | 辅助模块 LLM 接 DB 凭证 | P2 | - | pending (A 类 done) | A 类 9 处 done: `llm/service.py`(+resolve_for_context) + experience×4 + planning(document/planning/conflict) + project + artifact; B/C 类留 v6.24 |
| R1-R5 | OpenHands/Codex/构建/签名/BaaS 端到端 | - | 凭证/环境 | blocked | 各 adapter/deployer/baas (代码已实现, 待环境) |
| D1 | infra 变量前缀设计债务 | P2 | - | done (A 类) | `.env.example` 文件头+5 变量 [compose infra] 标注; B 类重命名 breaking 留专项 (forbid 已不触发, 不急) |
| D2 | conversation_config 子结构值对象化 | P3 | - | done | `domain/project/value_objects.py` (GitSyncConfig + LoopConfig) + entity 访问器 + session_manager/execution_helpers 裸读改造 |
| D3 | scanning 端点端到端集成测试 | P3 | - | done | `tests/integration/test_scanning_api.py` (fake scan_manager testdouble, 15 测试) |
| F1 | test_pipeline_e2e mock_llm 失效修复 | P2 | - | done | `tests/integration/test_pipeline_e2e.py` (mock_llm 双 patch) + `tests/integration/conftest.py` (_clear_adapter_pool_cache autouse); 集成套件 15min+flaky → 33s+216 全绿 |

## 关键决策 (待定)

## 对后续版本的约束

- 延续 v6.22: worker/扫描路径走 DB 凭证 (`acquire_worker`/`acquire_for_project`); resilience 4xx (非 429) 不重试
- 延续 v6.21: LLM 全局默认走 DB 用户默认凭证, env 兜底; `resolve_from_project` 统一入口
- 延续 v6.20 及之前既定约束

## 遗留 (本版本可能留)

- OpenHands/Codex 真注入 (blocked runtime)
- 多 worker Redis pub/sub (增强)
- 辅助模块 LLM 调用接 DB (渐进边界)

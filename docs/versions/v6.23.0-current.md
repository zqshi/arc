# v6.23.0 Current — (方向待定)

> 状态: 已激活 (2026-07-01, v6.22 归档后激活, 维持"有且仅有一个 *-current.md"不变量)
> 起始 commit: (归档激活, 2026-07-01)
> 上一版本: [v6.22.0](v6.22.0-snapshot.md)
> 上下文: v6.22 已归档 (v6.21 遗留收尾 + 扫描链路修复, D2/T5/T6/T7/T8 done + T2 评估不做)。本版本方向待用户细化。

## 下次会话接手清单 (2026-07-01 v6.22 归档后, 待定)

真相源本文件。基线 ruff0 / unit+integ 2448 passed / tsc -b 0 / vitest 97。工作区干净。

**本版本方向待定。** 候选 (从 backlog 技术债务 + 遗留):

| 候选 | 来源 | P | 说明 |
|------|------|---|------|
| conversation_config 子结构值对象化 | v6.22 技术债务 | P3 | git_sync/loop_config 防 7+ 处裸读拼写错误 |
| 辅助模块 LLM 调用接 DB | v6.22 遗留 | P2 | experience/pipeline/template/planning/artifact/gate 等仍走 env, 渐进边界 |
| scanning 端点端到端集成测试 | v6.22 6.6 记录 | P3 | scan_manager 全局单例难 mock, 需重构或 testdouble |
| infra 变量前缀设计债务 | v6.18 归档质检 | P2 | root .env.example 混合 arc 配置 + compose infra |
| OpenHands/Codex 真注入 | v6.19 遗留 | P1 | blocked runtime |

**推荐**: 等用户明确方向后细化任务依赖表。无既定 next.md 规划。

## 版本目标

(待用户细化)

## 版本验证标准

(待用户细化)

## 版本范围

**做什么:**
- (待定)

**不做什么:**
- (待定)

---

## 任务依赖图

```
(待定)
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 (核心) |
|----|------|---|------|------|----------------|
| (待定) | | | | | |

## 关键决策 (待定)

## 对后续版本的约束

- 延续 v6.22: worker/扫描路径走 DB 凭证 (`acquire_worker`/`acquire_for_project`); resilience 4xx (非 429) 不重试
- 延续 v6.21: LLM 全局默认走 DB 用户默认凭证, env 兜底; `resolve_from_project` 统一入口
- 延续 v6.20 及之前既定约束

## 遗留 (本版本可能留)

- OpenHands/Codex 真注入 (blocked runtime)
- 多 worker Redis pub/sub (增强)
- 辅助模块 LLM 调用接 DB (渐进边界)

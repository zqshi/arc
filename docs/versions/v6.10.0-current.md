# v6.10.0 Current — B 流程引擎内容编排（methodology/prompt/gate 可配置）

> 状态: 进行中 (v6.11 投产就绪归档后激活)
> 起始 commit: 674a4a9 (2026-06-27)
> 上一版本: [v6.11.0](v6.11.0-snapshot.md)

## ⚠️ 新会话启动清单（必读）

v6.11.0 已归档（commit 0a41592），质量检测 6.1-6.7 通过，CI 绿（ruff0/pytest unit 1936+integ133/tsc-b0/vitest88/build0）。**v6.10 三候选任务（methodology/prompt/gate 可配置）尚待 T1 设计，重量高风险。** 开 v6.10 前，建议先还掉以下 v6.11 遗留技术债（纯质量加固延续，且 TD-1 run() 属执行引擎核心，v6.10 动 prompt/gate 时可能触及，先拆干净降低耦合风险）。

### 推荐执行顺序

| # | 任务 | 说明 | 工作量 |
|---|------|------|--------|
| TD-1 | tool_loop.run() 补测试 + 拆分 ⭐ | `execution/tool_loop.py` `ToolAwareLoop.run()` 225 行超 80 行强制限（预存非 v6.11 引入）。**先补四分支（并行/串行/drift/error-loop）细粒度测试（Red→Green），再在测试保护下拆为 async generator 转发 yield**。当前 test_tool_loop_error_diagnosis 只覆盖 _execute_tool_with_retry/_diagnose_tool_error，裸拆回归保护不足 | 2-3h |
| TD-2 | 测试文件重复评估 | `test_deployment_service.py`（v6.11 T5 规范测试）与 `test_deploy_service.py`（混合：StorageAdapter+DeployService）内容部分重叠，都测 `_deploy_type_for`。建议 DeployService 部分并入前者，StorageAdapter 部分独立成 `test_storage_adapter.py` | 30min |
| TD-3 | tests 目录 ruff 清理 | 246 处预存 ruff（unused-import 等，CI 不跑 tests ruff）。`cd backend && ruff --fix tests` 后手动复核，只清 unused-import 类，勿让 ruff 改逻辑代码 | 15min |
| TD-4 | opensandbox SDK 本地处理 | `test_opensandbox_runtime.py` collection 失败（CI 有依赖，本地 6 skip 根因）。`pip install opensandbox` 或 conftest 固定 skip。非阻断 | 5min |
| → T1 | v6.10 B 方案设计 | TD-1~4 还清后，做 methodology/prompt/gate 可配置模型方案设计（见下） | - |

> 跳过还债直奔 v6.10 也行，但务必把 TD-1 在 v6.10 任务表显式列为前置依赖任务（而非技术债），避免在超限 run() 上叠加变更后期冲突面更大。

### v6.11 长期项（挂 backlog，非阻断）

86 TODO 逐条评估已完成：81 处在 seeds（纯数据占位符非代码 TODO），~4 处代码 TODO（execution 2/domain todo 1/experience 1），无安全/数据丢失阻断。可排进 backlog 专项，优先级低于 v6.10。

## 版本目标

将环节逻辑内容（methodology 方法论引导 / prompt 阶段提示词 / gate 门禁检查项）显性化为可配置模型，使流程引擎中枢从"硬编码 prompt + 规则门禁"演进为"内容可编排"。当前 methodology/prompt/gate 散落在代码常量与 provider 内部，新增/调整环节逻辑需改代码。本版本目标：让这些内容可声明管理（DB 持久化或配置驱动），不推翻现有 Provider 管道与7阶段骨架。

## 版本验证标准

1. 环节方法论（methodology）可按项目类型/环节声明，无需改 context/providers/methodology.py
2. 阶段提示词（PHASE_EXTRACTION_PROMPTS 等）可配置，存量行为不破
3. 门禁检查项（conversation_gate）可编排，门禁走 LLM 延伸（延续 v6.8 决策，非规则引擎）

## 版本范围（候选, 待细化）

**做什么:**
- [待设计] methodology 内容显性化 — context/providers/methodology.py → 可配置模型
- [待设计] prompt 内容显性化 — PHASE_EXTRACTION_PROMPTS / phase 提示词 → 可配置
- [待设计] gate 内容显性化 — conversation_gate 检查项 → 可配置
- 复用 v6.8 capability 注册表 / v6.9 类型驱动注册表模式

**不做什么:**
- 不推翻7阶段骨架（延续 v6.9 保骨架裁剪交付物决策）
- 不改 Provider 管道结构（内容可编排, 管道不变）
- 不做规则引擎门禁（延续 v6.8 LLM 延伸决策）

---

## 任务依赖图

```
TD-1 (tool_loop.run() 拆分, v6.11 遗留) ← 建议先还, 与执行引擎相关
TD-2/TD-3/TD-4 (轻量清理, 可并行, v6.11 遗留)
  └──→ T1 (B 方案设计: methodology/prompt/gate 可配置模型) ← 建议先还 TD-1
        └──→ T2+ 实施任务 (待 T1 设计后细化)
```

> B 需专门设计, 任务待细化。首会话: 先还 TD-1~4, 再设计可配置模型方案, 评估 DB 持久化 vs 配置文件 vs 混合, 再拆 T2+ 任务。

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| TD-1 | tool_loop.run() 补测试+拆分 (v6.11 遗留技术债) | P1 | - | pending | `backend/src/arc/application/execution/tool_loop.py`, `backend/tests/unit/application/test_tool_loop*.py` | 2-3h |
| TD-2 | 测试文件重复评估 (v6.11 遗留) | P3 | - | pending | `backend/tests/unit/application/test_deplo[y|ment]_service.py` | 30min |
| TD-3 | tests 目录 ruff 清理 (v6.11 遗留) | P3 | - | pending | `backend/tests/` | 15min |
| TD-4 | opensandbox SDK 本地处理 (v6.11 遗留) | P3 | - | pending | `backend/tests/unit/application/test_opensandbox_runtime.py` | 5min |
| T1 | B 方案设计（methodology/prompt/gate 可配置模型） | P0 | 建议先还 TD-1 | pending | 设计文档 | - |

> 激活本版本首会话: 先还 v6.11 遗留技术债(TD-1~4, 见顶部启动清单), 再完成 T1 方案设计, 再拆 T2+ 实施任务。B 重量高风险, 设计先行。

## 关键复用点（已核查）

- `context/providers/methodology.py` — 7阶段方法论 provider（待显性化）
- `PHASE_EXTRACTION_PROMPTS` — 阶段提示词常量（待可配置）
- `conversation_gate` / charter 门禁 — 门禁检查项（待可编排）
- v6.8 capability 注册表模式 + v6.9 类型驱动注册表（DELIVERABLES_BY_TYPE）— 内容编排可复用注册表模式

## 风险

- B 重量高风险: methodology/prompt/gate 是流程引擎中枢, 改动影响全环节。需专门设计, 评估存量兼容（双读过渡期, 同 v6.9 artifact 双读模式）
- prompt 内容可配置可能影响 LLM 输出质量 — 需回归测试保护

## 约束

- 延续 v6.9 约束; 双读兼容期不破坏存量
- 前端 tsc 检查用 `tsc -b --noEmit` 或 `npm run build`
- 新会话开始: 读本文件 + 执行会话级巡检(DDD 层级/循环依赖/文件超限)

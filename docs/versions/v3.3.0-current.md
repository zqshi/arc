# v3.3.0 Current — 功能集成 + 超限拆分

> 状态: 进行中
> 前置版本: [v3.2.0](v3.2.0-snapshot.md)

## 版本目标

1. 让 v3.0-3.2 建设的评审反馈系统真正接入主流程
2. 清理 3 个超限文件（拆分到 < 300 行）
3. route 层逻辑下沉到 service 层（todo.py 47 条件 / core.py 38 条件是重灾区）

## 版本验证标准

1. Pipeline gate check 后自动触发领域模型评审并持久化
2. `validate_domain_model` 路由改为调用 ReviewService
3. 所有非 seeds 文件 < 500 行
4. todo 路由文件条件数降到 < 15
5. 后端测试全量通过

## 版本范围

**做什么:**
- ReviewService 集成到主流程（DomainModelExtractor + validate 路由）
- planning_service.py 拆分（557行）
- routes/todo.py 拆分 + 逻辑下沉（557行, 47 条件）
- routes/project/core.py 拆分（513行, 38 条件）

**不做什么:**
- 测试补齐 → v3.4.0
- 前端 SUSPENDED UI → 后续
- 值对象建模（12 dict 字段） → 后续

---

## 任务依赖图

```
T1 (ReviewService 集成主流程) ← 独立, 关键路径
T2 (planning_service.py 拆分) ← 独立
T3 (routes/todo.py 拆分+下沉) ← 独立
T4 (routes/project/core.py 拆分) ← 独立
T5 (全量测试验证) ← 依赖 T1-T4
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 | 预估 |
|----|------|---|------|------|----------|------|
| T1 | ReviewService 集成主流程 | P0 | - | done | `operations.py` validate 路由接入 ReviewService, `artifact_extractor.py` 提取后自动评审 | 1h |
| T2 | planning_service.py 拆分 | P1 | - | done | 557→487行, 提取 `planning_experience.py` (118行) | 1.5h |
| T3 | routes/todo.py 拆分 + 逻辑下沉 | P0 | - | done | 557行单文件 → todo/ 目录: crud(186) + git(173) + conversations(121) + _helpers(68) + __init__(12) | 2h |
| T4 | routes/project/core.py 拆分 | P1 | - | done | 513→270行, 提取 `scanning.py`(122) + `github.py`(124) | 1.5h |
| T5 | 全量测试验证 + 回归修复 | P0 | T1-T4 | done | 948 tests all pass, import 路径完整, 路由数量正确 | 0.5h |


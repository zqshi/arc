# v6.5.0 Current — execution 层拆分 + 测试补全 + config 核对

> 状态: 进行中 (v6.4 归档后激活)
> 起始 commit: b7f9981 (v6.4 T7 归档后)
> 上一版本: [v6.4.0](v6.4.0-snapshot.md)
> 来源: v6.4 遗留技术债务 + prompt-upgrade 后续观察

## 版本目标

v6.4 收尾 prompt-upgrade 14/14 + CI 转绿后, 处理 v6.4 记录的 3 项技术债务, 保持代码库健康度。无新功能, 纯债务清理 + 健康度修复。

## 版本验证标准

1. execution 层 500-800 行文件评估拆分(超 600 拆, 500-600 记理由或拆)
2. ui_design_methodology 补 domain 单测(覆盖三态 gap, 含 T5 补的 error state)
3. config↔env 差 3 字段核对(补 env 或确认 derived 并文档说明)
4. CI 保持绿色, 全量测试不回归

## 版本范围

**做什么:**
- execution 层文件拆分评估: tools.py(601)/execution_engine.py(551)/artifact_extractor.py(546)/tool_loop.py(509) 逐个评估, 超 600 拆分, 500-600 评估职责边界
- ui_design_methodology domain 单测: 新建 test_ui_design_methodology.py(覆盖 empty/loading/error 三态 gap + wireframe 缺 user_story 标注)
- config↔env 核对: 50 字段 vs 47 env keys 差 3, 确认 derived 或补 .env.example

**不做什么:**
- 新功能开发 → 本版本是债务修复
- 大规模重构 → 只拆分超限文件 + 补测试 + 核对配置

---

## 任务依赖图

```
T1 (execution 文件拆分评估) ── 独立
T2 (ui_design 单测) ── 独立
T3 (config↔env 核对) ── 独立
T4 (质量检测) ─ blocked by T1-T3
```

## 任务详情

| ID | 任务 | P | 依赖 | 状态 | 涉及文件 |
|----|------|---|------|------|----------|
| T1 | execution 层文件拆分评估 | P2 | - | pending | tools.py/execution_engine.py/artifact_extractor.py/tool_loop.py |
| T2 | ui_design_methodology domain 单测 | P2 | - | pending | tests/unit/application/test_ui_design_methodology.py(新) |
| T3 | config↔env 差 3 字段核对 | P3 | - | pending | config.py/.env.example |
| T4 | 质量检测 | P0 | T1-T3 | blocked | 全量巡检 |

状态值: pending / in-progress / blocked / done

## 各任务实施要点

### T1 — execution 文件拆分评估
- 逐文件评估: 超 600 行(tools.py 601)优先拆, 500-600(其余)评估职责边界
- 拆分方向: tools.py 按工具类别拆; execution_engine 按 phase/职责拆; artifact_extractor 按提取类型拆; tool_loop 评估是否与 tools 合并
- 不强拆: 若职责内聚拆分收益低, 记理由保持(记入技术债务)
- 拆后跑全量 pytest 确认无回归

### T2 — ui_design 单测
- 新建 tests/unit/application/test_ui_design_methodology.py
- 覆盖: 三态 gap 检查(empty/loading/error, 含 v6.4 T5 补的 error state gap), wireframe 缺 user_story 标注
- domain 层零 mock, 直接构造 content dict 验证 validate_ui_design 行为

### T3 — config↔env 核对
- 50 字段 vs 47 env keys 差 3: 确认是 derived(无 env, 如 computed) 还是 .env.example 缺失
- derived: config 文档说明; 缺失: 补 .env.example 对应 key

## 对后续版本的约束

- execution 层文件保持 <600 行(拆分后), 新增代码注意职责内聚
- ui_design 等方法论模块必须有 domain 单测(零 mock)
- config 新增字段必须同步 .env.example

## 质量检测 (T4)

(待 T1-T3 完成后填写)
